# AGENTS.md — 给 AI 助手的工作指引

本仓库是**新院区总务交接班数字化系统**的文档与交互 Demo 仓库，目前**没有正式代码**。AI 在这里的工作以文档阅读、修改、评审为主。

## 上手阅读顺序

README → PRD §1 概要（60 秒版本）→ 台账 §二 编号规则 → 按任务深入对应文档。**开发实现类任务**再加读执行层三件：《任务分解》（工作队列）、《测试用例清单》（完成判据）、《API 契约》（接口与错误行为）。

## 权威层级（改东西前先看）

1. **规格台账**（规格编号与验收对照表）是唯一的需求索引：每条业务规则都有编号，开发、验收、变更、追溯一律以编号为准
2. **PRD** 是需求内容的完整出处；台账每条规格的"出处"列指回 PRD 具体章节（含版本）
3. **技术方案**是落地设计（架构、SQL、部署）；其 §4.3 关键口径必须与 PRD 附录 A 数据字典一致
4. **决策记录**解释"为什么这么定、当时还考虑过什么"；**改任何需求前先查它**，避免推翻已定决策而不自知
5. **执行层五件**（任务分解 / 测试用例清单 / API 契约 / 埋点与度量清单 / 开发种子数据）是规格层的操作面：任务从队列取（TK- 编号），完成判据 = 关联 `-T` 用例绿 + 台账回写；与规格层冲突时**以台账与 PRD 为准**并回改执行层文档

## 修改纪律（本仓库的铁律）

1. **编号只增不复用、不重排**：规格编号（C / F1–F7 / ELE / DATA / DEP）、决策编号（D-P / D-T / D-M）、任务编号（TK-）、用例编号（规格编号-T序号）、事件编号（EVT-）作废时保留编号加标记——规格标"(已废弃)"，决策标 ⛔——永不删除或复用
2. **修订记录只追加不删除**：PRD、技术方案、台账各有修订记录段落，每次改动新增一行，写清改了什么、为什么、谁拍板
3. **新需求 = 新编号**：给已有编号赋予新含义是禁止的；确需修改规格含义时在台账修订记录说明
4. **联动更新**：四份核心文档版本互相咬合，改其一须检查其余——
   - PRD 改版 → 同步检查技术方案受影响章节（尤其 §4.3 口径与数据库列定义）、台账对应规格行，必要时决策记录加条目
   - 台账规格新增/变更 → "出处"列指向 PRD 具体章节（含版本）
   - 各文档头部的"关联文档"版本指针一并更新
5. **状态符号不发明新的**：台账用 ✅ ⚠️ ⬜ ❓ ⏸ 🚫 📌，决策记录用 ✅ 🔓 ⛔，语义以各文档内的定义为准
6. **写代码时**：测试文件与用例名必须以 `-T` 编号挂钩台账（"需求即测试"）；接口行为与错误结构以《API 契约》为准，不一致改代码不改契约；种子数据以待确认项（❓）占位，不擅自定案数值

## Demo 的地位

demo/ 下两个 HTML（师傅端 v0.3、科长后台 v0.1）是**逻辑验证与验收依据**：台账"Demo 状态"列的 ✅/⚠️ 基于 2026-08-31 的验收记录。改动 Demo 交互逻辑须同步台账状态列；Demo 不是正式实现，正式系统仍须按台账逐条实现与验收。

## 当前阻塞项（改文档时不要当成已定案）

- ❓ DATA-12 液氧计量单位待总务科现场核实（阈值/用量/预警数值全局联动）
- 压力预警区间、月报导出格式、照片留存、肺功能气体基数待科长确认（台账"待确认清单"）
- 服务器资源、数据库实例、账号体系对接待信息科确认（技术方案 §9）

## 语言与格式约定

全中文撰写；表格用 Markdown；日期一律 `YYYY-MM-DD`；文件命名 `Handover-主题-v版本.md`；文档版本号放标题内（如"讨论稿 v0.2.8"），改版即升号，不覆盖旧版本内容。
