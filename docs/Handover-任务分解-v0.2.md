# Handover · 任务分解（v0.2）

- **定位**：把台账 108 条规格拆成**有序可执行的任务队列**——每个任务挂规格编号、写明完成判据与依赖，作为开发（人或 AI）的工作队列。台账写"要什么"，本表写"按什么顺序做、做到什么程度算完"。
- **上游文档**：《规格编号与验收对照表 v1.4》（范围与判据来源）、《技术方案与数据库设计 v0.2》（实现方式）、《测试用例清单 v0.1》（完成判据的用例索引）、《API 契约 v0.1》（接口边界）、《双轨比对操作规格 v0.1》与《手工验收与运营检查单 v0.1》（TK-32/33 验收工具）
- **编号规则**：`TK-` 前缀 + 序号，与规格/决策编号同纪律——**只增不复用**，作废保留编号标"(已废弃)"；执行顺序以表中排列为准，调顺序不改编号
- **完成判据的统一口径**：任务完成 = ① 关联规格的全部 `-T` 用例绿（见测试用例清单）② 台账对应规格行状态回写 ③ 涉及表结构/口径的改动在技术方案修订记录追加一行
- **生成时间**：2026-09-01
- **状态**：v0.2，按台账 v1.4 与 DEP-05/06 拆解；Phase 2 任务为骨架（⏸），启动前细化

**状态符号**：⬜ 未开始｜🚧 进行中｜✅ 已完成（用例绿 + 台账已回写）｜⏸ Phase 2 骨架

---

## 里程碑总览

| 里程碑 | 内容 | 出口判据 |
| --- | --- | --- |
| M0 地基 | 仓库脚手架、数据库落地、共享类型、认证 | 一条命令从零建库并启动开发环境 |
| M1 走通填写 | 登录 → 今日交接 → 板块填写 → 在线提交 | F1 在线链路 E2E 绿 |
| M2 计算防呆与离线 | 用量计算、防呆、离线队列、电梯核对 | F3 全部 P1 用例绿；断网注入用例绿 |
| M3 交接确认 | 带出、逐条知晓、签名、异议、撤回、定时任务 | F2 全部 P1 用例绿 |
| M4 管理后台 | 框架权限、记录/人员/排班/配置/电梯字典/审计 | F6 全部 P1 用例绿；科长可完成全部配置 |
| M5 度量与上线 | 埋点、E2E 全量回归、部署编排、双轨上线 | DEP-02 部署验收 + DEP-09 双轨比对通过 |
| P2 | 预警、报表、拍照、夜间窗口 | 台账 Phase 2 规格逐条 ✅ |

依赖说明：M2/M3 的定时任务与带出依赖排班**数据**（M0 种子已含），不依赖 M4 的排班**维护界面**，故 M3 可先于 M4。

---

## M0 地基（前置）

| 编号 | 任务 | 关联规格 | 完成判据 | 依赖 | 状态 |
| --- | --- | --- | --- | --- | --- |
| TK-01 | monorepo 脚手架：pnpm workspace，`apps/api`（NestJS）、`apps/h5`（师傅端）、`apps/admin`（科长后台）、`packages/shared`、`tests/e2e`（Playwright）；TS 严格模式、ESLint/Prettier、CI 空跑 | D-T01、DEP-01 | 一条命令启动全部开发服务；CI 上 lint + 空测试通过 | — | ✅ |
| TK-02 | 数据库落地：Drizzle schema 按技术方案 §4.2 十二张表逐列建齐，迁移脚本 + 种子加载脚本（数据见《开发种子数据 v0.1》） | DATA-11、技术方案 §4 | 全新 MySQL 实例一键 migrate + seed 成功；schema 与 §4.2 DDL 逐列核对无异 | TK-01 | ✅ |
| TK-03 | 共享类型与错误契约：`packages/shared` 定义板块/字段/状态枚举与**统一错误响应结构**（含 `missing_fields[].anchor`，见 API 契约 §2）；三端同源引用 | C-09、DATA-11 | 三端 import 编译通过；错误结构类型与 API 契约一致 | TK-01 | ✅ |
| TK-04 | 认证与账号：登录/登出/当前用户接口，bcrypt 加盐，会话超时，登录事件写审计（设备、IP） | F1-11、C-05 | F1-11-T1/T2/T3 绿 | TK-02 | ✅ |

## M1 走通填写

| 编号 | 任务 | 关联规格 | 完成判据 | 依赖 | 状态 |
| --- | --- | --- | --- | --- | --- |
| TK-05 | 今日交接首页：一天一条记录；12 张任务卡按点位组织、液氧拆 8:30/20:30 两张到点卡；角标与顶部进度条实时汇总 | F1-01、F1-02、F1-03 | F1-01-T1、F1-02-T1/T2、F1-03-T1 绿 | TK-04 | ✅ |
| TK-06 | 板块填写页与校验：十板块表单按 PRD 附录 A 逐字段；必填/数值范围校验；**报错点名缺失字段清单 + 点击跳转定位**；单位全程标注 | F1-08、C-09、DATA-01 | F1-08-T1/T2/T3、DATA-01-T1 绿 | TK-05 | ✅ |
| TK-07 | 上一班读数带出：按 `duty_date` 取前一条已提交记录回填显示；液氧 8:30 带出昨日 20:30 值；首班无数据显"—"提示 | F1-05、F1-15、DATA-02、F3-07 | F1-05-T1/T2、F1-15-T1、F3-07-T1（缺失态契约；补录与计算随 TK-14 复验）绿 | TK-06 | ✅ |
| TK-08 | 草稿自动保存与续填：IndexedDB 草稿层，几秒自动保存，重开恢复。**评审遗留关注项（TK-06 评审 M5，2026-09-10 二次评审订正）：草稿 tombstone 语义**——**持久化草稿必须按用户（submitter）与班次 keying，其清除只随「本人登出/本人会话失效」发生，登录请求自身的 401（F1-11-T2/T3 密码错/停用）不得清除本机草稿**（否则输错一次密码即丢草稿，违反 F1-09-T1 续填）；会话内内存草稿的清空收敛到 App.vue `resetSession` 单一入口（被动掉线与 bootstrap 静默失败路径已在 TK-06 预落），不得另开清空路径；**另挂 TK-07 评审遗留（m1/m2）**：SectionView 的 /prev 拉取提升到 App 级按 duty_date 缓存（当前每开一卡重复拉取）、其 401 上抛并入 resetSession 单一入口（当前静默吞错） | F1-09 | F1-09-T1 绿 | TK-06 | ✅ |
| TK-09 | 液氧板块专项：两罐两时点 8 项读数必填（DATA-01 已由 TK-06 归位恒必填）；使用罐号枚举驱动卡片标题"在用/备用"（shared `tankRoleOf` 三端同源；SectionView 行标签 + 首页液氧两卡标题后缀）；**填写时自动记录实际测量时刻（含离线本地时间戳，shared `measuredAtTarget`/`localMeasuredAt`，随草稿持久化）**；日间用量按在用罐取数（shared `calc.ts loDayUseOf`，TK-13 复用） | DATA-01、DATA-03、DATA-04、DATA-13 | DATA-03-T1、DATA-04-T1、DATA-13-T1/T2（客户端半边；服务端「不被同步时刻覆盖」复验挂 TK-12）绿 | TK-06 | ✅ |
| TK-10 | 锅炉停机联动：停机时使用锅炉号/出水/回水温度置灰不填、不参与必填校验 | DATA-05 | DATA-05-T1（接口层契约级先行 + 客户端半边 E2E；TK-12 supertest 复验）绿 | TK-06 | ✅ |
| TK-11 | 新风多选：使用位置多选，候选清单读 configs（含配置只读端点）。**落地口径：GET /configs 表单选项白名单只读端点**（api configs 模块，只暴露 hvac_locs/boiler_list 清单键，运营键不出网；脏配置回落空数组不阻塞）**+ h5 候选接入**（SectionView hvac_locs 多选与 boiler_no 枚举取该端点，App 级拉取缓存、不可达时降级种子占位；boiler_no 一并接入系 SectionView 既有注释承诺） | DATA-07 | DATA-07-T1（接口层契约级先行：候选与 configs 同步 + 白名单收敛 + 脏配置降级；多选数组落库复验挂 TK-12 supertest）绿 | TK-06 | ✅ |
| TK-12 | 在线提交与预览：提交前汇总预览（未填项/异常项）；提交生成 record_no、服务端固化用量、状态转 submitted；接班人按排班自动带出、可改须填原因留痕。**评审遗留关注项（TK-06 评审 M5）：parseNumeric 现走 `Number()` 宽松口径**（`'0x10'` 会被当 16 放行），提交校验须收紧为十进制字面量；C-09 锚点须保证跨卡唯一；**录入上限由 shared `FIELD_PRECISION` 派生**（max = 10^(p−s) − 10^−s，INT 列取整），提交校验时与 `FIELD_RANGES` 运营区间合流使用（TK-06 评审 M4 挂账，本轮只挂账不接入）；**评审遗留关注项（TK-08 评审）：提交成功后须清本机持久草稿（`draft.markSubmitted`，D-T18 修订 #9），否则重开页面旧草稿复活并以「草稿 ?? 服务端值」优先于服务端真值（撤回重提后同）**；**评审遗留关注项（TK-09）：DATA-13-T1/T2 服务端半边——`lo_measured_am/pm` 由客户端随 payload 上送、服务端原样落库，不得以同步/接收时刻覆盖（D-P12；契约 §4 补充口径，客户端半边已由 TK-09 落地）**；**评审遗留关注项（TK-10）：DATA-05-T1 接口层复验——停机（boiler_run='stop'）整卡放行、运行缺项 400 逐条点名（supertest 对 records-boiler.spec.ts 同组场景，台账增补 #15）；**submit 时 boiler_run='stop' → 三项强制写 NULL**（台账 DATA-05 原列「停机列留空」字面口径：客户端选停机清列为第一道、服务端落库为第二道——撤回重提/异议重提/离线重放时服务端旧值不因本机草稿为空而复活，增补 #16）；**评审遗留关注项（TK-11）：DATA-07-T1 服务端落库半边——hvac_locs 多选值以 JSON 数组原样落库（前端候选数据源 GET /configs 已由 TK-11 落地，契约 §3.7）** | F1-10、F2-01、F1-08、DATA-01、DATA-05、DATA-09、DATA-10、DATA-13 | F1-10-T1、F2-01-T1、**F1-08-T2、DATA-01-T1**（接口层复验，supertest 对 shared 校验引擎同组场景）、DATA-09-T1、DATA-10-T1、**DATA-13-T1/T2（服务端不覆盖复验，TK-09 挂账）**、**DATA-05-T1（接口层复验，TK-10 挂账）**、**DATA-07-T1（数组落库复验，TK-11 挂账）** 绿 | TK-06、TK-07 | ✅（2026-09-12 落地，修订 #22：挂账七项全部消化——parseNumeric 收紧/精度上限接入、markSubmitted、DATA-13 不覆盖复验、DATA-05 强制 NULL 复验、DATA-07 数组落库复验、F1-10/F2-01/DATA-09/10 判据全绿；修改接班人候选 UI 挂 TK-26、duty_guard 判定挂 TK-14、用量固化挂 TK-13） |
| TK-39 | Demo v0.4 状态补齐：师傅端 demo 补三个关键界面状态——C-09 报错点名+跳转、离线态（待同步/已同步）、首班"—"态（UI 状态缺口以 demo 迭代落地，不另写独立 UI 规格文档） | C-09、F1-08、F1-06、F1-15 | demo 三态演示可走通；台账 F1-08 备注复核；Demo 状态列同步 | —（独立于正式开发，随时可做） | ⬜ |

## M2 计算防呆与离线

| 编号 | 任务 | 关联规格 | 完成判据 | 依赖 | 状态 |
| --- | --- | --- | --- | --- | --- |
| TK-13 | 用量计算引擎：水/电（两线和、分线展示）/气/液氧日间四类口径按 §4.3 纯函数实现，提交时服务端计算固化；结果标"自动计算"；手工覆盖强制原因留痕。**落地口径：shared `calc.ts` 四类纯函数为单一权威实现**（`waterDayUseOf`/`eDayUseOf` 分线+合计/`gasDayUseOf` 剩余量减少值合计/`loDayUseOf` 复用 TK-09），上一班取数与 GET /records/today/prev 共用 `adjacentPrevRow`（D-T17，不回落）；**覆盖协议 `usage_overrides[]`**（reason 必填，空白/非法值/越键 400 逐条点名；合法覆盖以覆盖值固化并写审计 `record.usage_override`，old_value 记自动值——即 TK-16 重算豁免的判定依据）；GET /records/today 字段状态增 `manual` 旗标（F3-06-T1「自动计算」标识与人工值可区分）；h5 实时预览同源（水显差值、电/气分线同屏，非权威）+ lo_day_use 覆盖入口（原因随草稿持久化，`usage_override_reason` 为草稿层非字典键——TK-14 起**另**有 `prev_backfill:*`，本行原「唯一」表述作废，见修订 26/27）；上一班缺失 → 用量列固化 null（补录随 TK-14）；充气确认后单卡按 0 计（D-P14）随 TK-14 接入 | F3-01、F3-02、F3-03、F3-04、F3-06 | F3-01-T1、F3-02-T1、F3-03-T1、F3-04-T1/T2、F3-06-T1/T2 绿（单元+接口层 `records-usage.spec.ts` 17 项） | TK-12 | ✅（2026-09-12 落地见修订 #24；独立评审修复轮 M1–M4+L1–L6 全修、L2 宽口径定案见修订 #25） |
| TK-14 | 防呆三则：读数小于上一班强制确认；天然气单卡剩余量增大充气确认（该卡按 0 计、另一卡正常）；上一班缺失显"—"允许补录。确认走 409 + 确认重提协议（见 API 契约）。**落地口径（修订 26）：判定层 shared `guard.ts`（判定范围定案 D-T19）；补录走 payload `prev_readings`（仅缺失态消费，审计 `record.prev_backfill` 留痕，不落 records 列）** | F1-12、F1-13、F3-07 | F1-12-T1/T2、F1-13-T1/T2、F3-07-T1 绿 | TK-13 | ✅ |
| TK-15 | 离线三层缓冲：待同步队列/照片暂存区（**草稿层已由 TK-08 落地**，本任务不再包含；清除口径见 D-T18 修订 #9）；离线提交仅入本地队列并提示；恢复网络顺序上传、同步状态全程可见；长时间未同步次日强提醒。**落地口径（修订 28）：队列边界定案 **D-T20**——h5 `store/sync-queue.ts`（IndexedDB `sync_queue` store，DB_VERSION 2 升版 + `onblocked` 回调兑现 TK-08 挂账；`{user_id}:{duty_date}` keying、`queued_at` 顺序键）；离线「提交」降级为本地同口径预检（shared validateFields / `unconfirmedNeedConfirmItems`——后者系本轮自 records.service 下沉 shared 的 need_confirm 组装单一权威实现，api 提交侧同步改为消费，行为不变），防呆确认前置到入队前；排空引擎（online 事件/登录就绪/手动按钮）按序上传，待同步期间草稿仍为权威数据源、payload 随自动保存刷新、清除时点=同步成功；同步角标/横幅全程可见；滞留强提醒以 `duty_date` 早于当前班次为准、全屏不可忽略 overlay 门控自动排空；照片暂存区随 TK-36，EVT-04/05 上报挂 TK-30。**评审修复轮（修订 29）**：M1 跨班次滞留单禁止上传（服务端只认当前班次，上传必然落错日期；补交端点挂待拍板决策）、M2 自愈加键、M3/M5 统一冲账清草稿+会话代次+非网络异常不静默、M4 同步在途锁编辑+在途补改显式提示、M6 离线预检始终执行+确认入队即清、L1 补录校验下沉 shared、L2 重试上限+overlay 科长指引、L3 手动同步离线反馈、L4 onversionchange+开库超时、L5 登录页本机未同步单提示、L6 补丁失败可见 | F1-06、F1-07、F1-14 | F1-06-T1、F1-07-T1/T2、F1-14-T1（断言已按 M1 翻转：跨班次项零上传请求）+ 修复回归 5 项（M3/M5、M4、M6、L3、L5）绿（E2E `sync-queue.spec.ts` 9 项；F1-07-T2 层级说明见台账增补 #25） | TK-12 | ✅（2026-09-13 落地见修订 #28；独立评审修复轮见修订 #29 与台账增补 #26；门禁 build 三端/lint/format/jest 154/154 重灌种子/E2E 全量 45/45/check-ledger 全绿） |
| TK-16 | 下游重算与豁免：上一班晚到（离线补同步）或异议修改读数后自动重算下游用量并写审计；师傅手工覆盖值豁免不被覆盖。**落地口径（修订 30，D-T21 拍板）**：晚到触发点 = 新增路由 `POST /records/backfill`（跨班次补交：duty_date 显式且严格早于当前班次、提交人恒为登录人本人、409 RECORD_EXISTS、其余校验/防呆/覆盖/补录协议与 submit 同口径——submit 重构为 submitCore 单一实现，补交同口径复用）；补交成功后**同事务**重算紧邻下游 D+1 已提交记录的 water/e/gas 三项 prev 依赖用量（四类口径均只依赖紧邻上一班，故范围恰为 D+1；液氧日间用量不在范围）；豁免 = 当前版本 `record.usage_override` 审计行（D-T07，与 F3-06-T1 manual 旗标同源）；值无变化不更新不写审计；下游原提交确认充气的卡延续按 0 计（D-P14，从 record.submit 审计反查）；审计 `record.late_submit` + 逐变更项 `record.recalc`；异议修改（TK-20）触发重算复用同一 recalcDownstream 服务方法；F6-06 漏交检测以 records 行存在为准，补交日自此不再计漏交；h5 排空引擎的滞留单处置入口接入补交端点（「联系科长」→ 自助补交）挂 TK-24/后续，本任务不动 h5。**评审修复轮（修订 31，D-T21 修订四条拍板）**：M1 重算「值是否变化」改**数值判等**（原字符串判等使整数用量恒被误判为变更、假审计成倍产生，探针实证）+ L1 无变更时 recalc=null + **L2 只重算 status='submitted'**（不静默改 objection/completed 签名归档件）+ **L3 补交窗口**（duty_date 下限 = 当前班次 − configs `backfill_window_days`，种子 7、❓ 待确认第 10 项）+ **L5 draft 接管**（仅非 draft 行 409，消除撤回后永久无法入库的死角）+ **L6 待复核清单**（新基线命中 D-T19 判定时不改数不拦提交、标 `recalc.needs_review` + 审计 `record.recalc_review`）；挂账：无 backfill/preview（随 TK-24）、补交班次归属校验（挂 TK-26）、重算行锁与 1062→409（随 TK-20 复用前必补） | F3-08 | F3-08-T1/T2 绿（接口层 records-backfill.spec **13 项**：重算黄金值 + 审计、**M1/L1 零变更零审计**、L2 ×2（completed/objection 不重算）、L3 窗口越界、L5 draft 接管、L6 待复核、chief 口径、duty_date 约束、RECORD_EXISTS、防呆 409 同协议） | TK-13 | ✅（2026-09-13 落地见修订 30；**2026-09-14 独立评审修复轮见修订 31**（M1 探针实证 + L1/L2/L3/L5/L6 全处置、四项由用户拍板）；D-T21 待拍板项已闭环，联动契约订正 17/18、技术方案修订 22/23、台账增补 #27/#28） |
| TK-17 | 电梯核对（表单端）：打开板块按核对时刻生成逐台"预期状态"并**锁定**；一致一键确认、不一致选实际状态必填说明；跨零点时段支持。**落地口径（修订 33，D-T22 拍板）**：GET /elevators/expected 只读计算不落库（原契约「生成明细行」写副作用与 D-T18 冲突）；核对结果随提交 payload `elevator_checks[]` 在 submitCore 同事务落库（离线随队列可用）；服务端按上送 check_time 重算 expected（ELE-05「提交时不重算」=不按提交时刻重算，D-P16 字面）；任一不一致写 alerts 标红行 level=mid，说明缺失 409 逐台 `elevator:{id}` 点名（shared validateElevatorChecks 三端同源：api 校验与 h5 卡内/离线预检同一实现）；电梯卡角标维持 0/0（D-T16 分母为字段维度） | ELE-02、ELE-03、ELE-04、ELE-05、ELE-06、ELE-07、ELE-09 | ELE-02-T1、ELE-03-T1、ELE-04-T1/T2、ELE-05-T1、ELE-06-T1、ELE-07-T1、ELE-09-T1 绿（records-elevator.spec 16 项：单元 6 + 接口 10；E2E elevator-check.spec 3 项客户端半边） | TK-06 | ✅（2026-09-14 落地见修订 33，**独立评审修复轮见修订 34**；门禁 build 三端/lint/format/jest 185/185 重灌种子/E2E 全量 47/48 + 单跑复绿（draft-persist flake 族同 #21）/check-ledger 全绿） |

## M3 交接确认

| 编号 | 任务 | 关联规格 | 完成判据 | 依赖 | 状态 |
| --- | --- | --- | --- | --- | --- |
| TK-18 | 待确认入口与逐项浏览：接班人登录醒目入口；逐项浏览，标红项置顶高亮。**落地口径（修订 35）**：`GET /records/pending`（我为 receiver 且 status=submitted）+ `GET /records/{id}` 详情（alerts 置顶序 + 全读数 + 电梯核对明细，F2-03/F5-01 共用）；提交协议第 ④ 步补全（状态异常/交接事项拆条标红行，契约 §4 原文） | F2-02、F2-03 | F2-02-T1、F2-03-T1 绿 | TK-12 | ✅ |
| TK-19 | 逐条知晓与签名归档：标红项（状态异常/电梯不一致）与交接事项逐条"已知晓"后方可签名；确认完成归档，双方姓名/时间/签名可查。**落地口径（修订 36）**：`POST /records/{id}/acknowledge`（body `alert_ids[]` 逐条写 acknowledged_by/at，幂等不覆盖首次时刻，仅接班人本人 403 拦非本人）+ `POST /records/{id}/confirm`（签名图 PNG data URL 解码校验 → 落盘 `/uploads/signatures/{record_no}.png`、confirmed_at=服务端时刻、完整性终校与转 completed 同事务 409 CONFIRM_INCOMPLETE、审计 `record.confirm`）+ h5 逐条"已知晓"按钮与签名板（C-05 姓名二次确认） | F2-04、F2-05、DATA-08、DEP-08 | F2-04-T1/T2、F2-05-T1、DATA-08-T1、DEP-08-T1 绿 | TK-18 | ✅ |
| TK-20 | 异议与版本：标注异议+原因退回交班人；修改重提版本 +1，record_versions 留全字段快照与变更。**落地口径（修订 37，D-T23 拍板）**：`POST /records/{id}/objection`（仅接班人本人 403/D-P06，note 必填，行锁下转 objection）+ `GET /records/mine/objections`（交班人待办）+ `PUT /records/{id}`（部分合并直写行、首改快照入 record_versions、changed 累计口径）+ `POST /records/{id}/resubmit`（version+1、重走校验/防呆/计算、alerts 重建、下游 D+1 重算 trigger=objection_resubmit）；历史版本经 GET /records/{id} versions 摘要可查；**TK-16 挂账闭环**（下游行锁 FOR UPDATE + 1062→409 归一，契约订正 18 ⑧） | F2-06、F2-07 | F2-06-T1、F2-07-T1 绿（接口层 records-objection.spec 5 项） | TK-19 | ✅（2026-09-15 落地见修订 37） |
| TK-21 | 撤回窗口：10 分钟倒计时入口；三不可撤条件服务端校验并提示走异议流程；撤回留痕、接班人端入口同步消失。**落地口径（修订 38）**：`POST /records/today/withdraw`（仅交班人本人，按当前班次 C-08+submitter 定位；行锁下三不可撤条件状态锁定先于窗口，409 WITHDRAW_NOT_ALLOWED 携 `reason` WINDOW_EXPIRED/ALREADY_CONFIRMED/IN_OBJECTION；成功转 draft+清 submitted_at+同事务清 alerts/elevator_checks、version 不变，审计 `record.withdraw`）+ GET /records/today 回传 `withdraw_window_minutes`（服务端读 configs 回落 10）+ h5 首页撤回按钮带倒计时（到期自隐）；重提走 submitCore draft 分支 version+1 | F2-08、F2-09、F2-10 | F2-08-T1/T2、F2-09-T1/T2、F2-10-T1/T2/T3 绿（接口 records-withdraw.spec 8 项 + E2E withdraw.spec 2 项） | TK-12 | ✅ |
| TK-22 | 服务端定时任务四件：接班人 2 小时未确认提醒；异议 24 小时升级科长（防重复）；按排班扫描应提交未提交提醒（默认次日 9:00，时点可调读配置）；清理 sessions 过期存根行（技术方案 §6，运维卫生类、无对应台账规格）。时间可注入。**落地口径（修订 39）**：`notifications` 模块（apps/api/src/notifications）——四扫描 `runConfirmDueScan`/`runObjectionEscalationScan`/`runMissingSubmitScan`/`runSessionsCleanup` 的 **now 一律由调用方注入**，生产由 @nestjs/schedule 按间隔喂系统时钟（5/30/30/60 分钟，jest 环境调度静默）；F2-11 去重键 (kind,record,receiver) 带 submitted_at 水位（notifications.createdAt 系库端 UTC 串，与 records 列本地墙钟串比较经 shared localTimestampToDate 换算）；F2-12 防重复 = escalated_at 原子闸门（UPDATE … WHERE escalated_at IS NULL），提醒全部 active 科长；F6-06 漏交判定以 records 行存在为准（增补 #27）、扫描窗口 = 当前班次 − backfill_window_days（窗口外无法补交不再提醒）、去重键 (chief,title)（title 携带 duty_date）、截止判定在墙钟空间（duty-date.ts 新导出 localWallClock）；读侧契约 §3.5 GET /notifications + POST /notifications/{id}/read 随本轮落地（权威形状 shared dto） | F2-11、F2-12、F6-06 | F2-11-T1、F2-12-T1、F6-06-T1 绿（接口层 notifications.spec 7 项，含时点可配两轮扫描、窗口外不提醒、会话清理与闸门回归） | TK-12（排班数据由种子提供） | ✅ |

## M4 管理后台

| 编号 | 任务 | 关联规格 | 完成判据 | 依赖 | 状态 |
| --- | --- | --- | --- | --- | --- |
| TK-23 | 后台框架与权限：PC Web 布局；chief 角色守卫，师傅访问后台接口一律 403。**落地口径（修订 40）**：API 新增 admin 模块——契约 §3.6 科长路由统一**类级**守卫（SessionGuard + RolesGuard + `@Roles('chief')`），master 访问一律 403；首条落地 GET /admin/missing-submits（F6-06 后台视图半边，漏交判定与通知同源 missingSubmitShifts 单一实现，权威形状 shared `MissingSubmitListDto`）+ **路由完备性哨兵**（已注册 /admin 路由逐字对账契约清单，TK-24~29 新挂路由未进矩阵即红）；apps/admin 骨架——Element Plus PC 布局四页导航（记录管理/人员与排班/配置中心/审计日志）、Cookie 登录、/auth/me 会话恢复、role≠chief 无权限页、401 单一入口（不引 vue-router，与 h5 同款组件状态切换）；E2E 新增 desktop-admin 项目 | C-05、F6 | 权限用例：master 调 admin 路由全部 403（admin.spec 8 项 + E2E admin-shell 3 项） | TK-04 | ✅（2026-09-16 落地见修订 40） |
| TK-24 | 记录管理：查看/按日期与交班人筛选/导出/批注。**落地口径（修订 41，D-T24）**：GET /records 列表（契约 §3.5 既有行，F6-01 科长半边落地、F5-01 界面仍 P2/TK-35）——from/to/submitter_id/status 四参数筛选（解析 parseRecordListFilters 单一实现，与导出共用；非法 400 逐条点名）、duty_date 倒序、不分页；GET /admin/records/export = CSV 附件（UTF-8 BOM + CRLF，14 列，文件名按筛选区间；xlsx/正式月报挂 P2 F5-04）；POST /admin/records/{id}/annotation 批注（**契约订正 28 新增路由 34→35**：覆盖式单条 records 新列 chief_note、空串=清除、500 字上限、不限记录状态、值无变化不写审计、审计 record.annotate 留痕、不进 record_versions）；apps/admin 记录管理页（默认近 30 天 + 交班人/状态筛选 + 列表 + 详情抽屉（标红/十板块读数/批注区）+ 批注对话框 + CSV 下载）；挂账移交：科长代录他人与 /records/backfill/preview（无消费者）移挂 h5 滞留单处置面板落地时评估（D-T24） | F6-01 | F6-01-T1 绿（接口层 admin-records.spec **15 项** + E2E admin-records.spec 3 项） | TK-23 | ✅（2026-09-17 落地见修订 41） |
| TK-25 | 人员管理：账号开通/停用，写审计；停用即不可登录 | F6-02 | F6-02-T1 绿 | TK-23 | ⬜ |
| TK-26 | 排班管理与安全阀：最小排班表"日期→人"月视图维护、改即审计；驱动带出与漏交检测；登录提交人≠当日排班时确认后方可提交并留痕 | F6-03、F6-04、F6-05 | F6-03-T1、F6-04-T1、F6-05-T1/T2 绿 | TK-23 | ⬜ |
| TK-27 | 配置中心：阈值/基数/区间/新风位置清单/锅炉清单统一维护，新旧值写审计、保存即全员生效；点位字典维护 | F6-07（P1 部分）、C-07 | 配置修改后下一班表单即时生效（接口用例）；审计断言新旧值完整 | TK-23 | ⬜ |
| TK-28 | 电梯字典后台：三选一运行计划（时段配置器支持跨零点）、停运原因、增删改写审计；新增电梯当日表单立即出现 | ELE-01、ELE-08、ELE-09 | ELE-01-T1、ELE-08-T1 绿 | TK-27 | ⬜ |
| TK-29 | 审计日志查询：按动作/操作人/时间筛选，展示谁何时把什么从多少改成多少 | F6-08 | F6-08-T1 绿 | TK-23 | ⬜ |

## M5 度量与上线

| 编号 | 任务 | 关联规格 | 完成判据 | 依赖 | 状态 |
| --- | --- | --- | --- | --- | --- |
| TK-30 | 埋点与度量采集：按《埋点与度量清单 v0.1》落 telemetry_events 表与客户端事件上报；PRD §4 五项指标可由库内数据直接算出 | DEP-11、埋点清单 | 指标口径表的 SQL 在种子库上跑通并出五项指标 | TK-12 | ⬜ |
| TK-31 | E2E 全量回归：Playwright 用例库覆盖测试用例清单全部 P1 条目（登录→填写→防呆→提交→确认→签名→归档全链路 + 断网注入）；每次提交自动回归 | 技术方案 §8 | 用例清单 P1 条目全绿，报告归档 | M1–M4 | ⬜ |
| TK-32 | 部署编排：Docker Compose 三容器（Nginx/后端/MySQL）；院内自签 HTTPS；每日备份脚本 + 恢复演练流程；升级冒烟（跑核心用例） | DEP-02、DEP-03、DEP-04 | 部署验收通过（《手工验收与运营检查单 v0.1》§三 DEP-01~04 全签字）；全新服务器一条命令拉起，冒烟通过 | TK-31 | ⬜ |
| TK-33 | 上线流程：与科长确认全部阈值/基数/字典/清单 → 录入生产配置 → 按《双轨比对操作规格 v0.1》执行双轨并行两周逐日比对 → 按《手工验收与运营检查单 v0.1》完成培训与上线核验 → 过渡期微信群转发预警摘要 | DEP-09、DEP-10、DEP-12 | 双轨转单轨五条件满足（比对表归档、科长签字）；检查单 §五/§六 全签字；预演一次完成 | TK-32、待确认清单闭环 | ⬜ |

## P2 Phase 2（骨架，启动前细化）

| 编号 | 任务 | 关联规格 | 完成判据/说明 | 依赖 | 状态 |
| --- | --- | --- | --- | --- |
| TK-34 | 预警规则引擎与预警中心 | F4-01～F4-12 | 配置驱动求值器；提交时逐项 + 定时复核；预警文案含规则与阈值（C-03）；推送科长 | — | ⏸ |
| TK-35 | 历史趋势与月报导出 | F5-01～F5-04 | 曲线点击跳转当日单；Excel 导出（格式待确认 F5-04） | — | ⏸ |
| TK-36 | 可选拍照留证 | F7-01～F7-03 | 照片挂字段、压缩约 500KB、离线随表单同步 | — | ⏸ |
| TK-37 | 液氧夜间窗口监控 | F3-05、F4-09 | 跨记录夜间差值（口径已定案）；换罐日跳过并标注；12 小时窗口降幅防泄漏 | — | ⏸ |
| TK-38 | 预警复盘标记 | C-07、DEP-11 反指标 | alerts 增复盘结果字段，支撑“预警准确率”月度复盘 | — | ⏸ |

---

## 使用说明

1. **取任务**：从队首取第一个未完成任务；同里程碑内按依赖列约束，无依赖冲突时可并行
2. **完成即回写**：任务 ✅ 时同步回写台账状态列；台账状态是"未勾完的模块不发布"的执法依据，本表不替代台账
3. **新需求先进台账**：需求变化先在台账新增/修订规格（编号纪律见台账 §二），再到本表新增 TK 任务——顺序不可反
4. **顺序可调、依赖不破**：M2 与 M3 可交错；M4 仅 TK-26（排班）被 M3 的定时任务间接依赖数据（种子已解耦）

---

## 修订记录

1. **v0.1（2026-09-01）**：初稿。按台账 v1.3 与 DEP-05/06 拆解为 38 个任务（P1 33 个 + P2 骨架 5 个），组织为 M0–M5 六个里程碑；完成判据统一挂钩测试用例清单的 `-T` 编号。
2. **v0.2（2026-09-01）**：TK-33 交付物正式挂接《双轨比对操作规格 v0.1》与《手工验收与运营检查单 v0.1》（原括号内提示升级为独立文档）；TK-32 判据引用检查单 §三；新增 TK-39（demo v0.4 状态补齐——UI 状态缺口以 demo 迭代落地）；上游台账指针同步至 v1.4。
3. **v0.2 状态回写（2026-09-01）**：TK-01 完成（⬜→✅）——仓库根建 pnpm workspace 五包（apps/api、apps/h5、apps/admin、packages/shared、tests/e2e），TS 严格模式 + ESLint + Prettier + GitHub Actions CI 工作流；本地验证 `pnpm dev` 一命令三服务启动（api:3000/h5:5173/admin:5174）、lint 与空测试全绿。队列下一个任务：TK-02（数据库落地）。
4. **v0.2 状态回写（2026-09-02）**：TK-02 完成（⬜→✅）——Drizzle schema 按 §4.2 十二表逐列建齐（records 75 列），drizzle-kit 迁移 + 手工对齐段（ENGINE/COMMENT/ON UPDATE）与 §4.2 DDL 逐列核对无异；db:migrate 自动建库、db:seed 幂等落地种子全量数据（计数自检：5/22/11/17/8/10 均吻合）；配套修订《开发种子数据》电梯占位窗口对齐场景矩阵。队列下一个任务：TK-03（共享类型与错误契约）或 TK-04（认证，依赖本任务）。
5. **v0.2 状态回写（2026-09-02）**：TK-03 完成（⬜→✅）——`packages/shared` 分层落地五模块（enums/sections/fields/errors/index）：十板块定义（附录 A）、records 全字段字典（板块/label/单位/填写方式，DATA-11）、13 个错误码 + 统一 `ApiError` 结构（`missing_fields[].field/section/label/anchor` + `need_confirm` + `request_id`，C-09 落地《API 契约》§2）、`fieldAnchor`/`toMissingField` 锚点与点名构造；`index.ts` 以 `satisfies ApiError` 固化契约 §2 示例作编译期一致性证明。验证：`pnpm build` 三端（api `nest build`、h5/admin `vue-tsc`+`vite build`）全绿，`pnpm lint`、`pnpm test` 通过。发现两处上游文档待订正（不在本任务擅改）：《API 契约》§2 修订记录「错误码表 14 项」与实际表 13 项不符；本表 TK-03 行「见 API 契约 §3」应为 §2（错误结构在 §2）。队列下一个任务：TK-04（认证与账号，依赖 TK-02 已就绪）。
6. **v0.2 状态回写（2026-09-02）**：TK-03 代码 review 后修正四处——① 字段字典补 `lo_night_use`（液氧夜间用量，PRD v0.2.8 定案的跨记录派生值，非 records 存储列；表单展示与 F5-02 趋势需其 label/单位故纳入）；② `fieldAnchor` 参数收窄为 `RecordFieldName`（防拼写错误静默通过）；③ errors 补请求侧确认类型 `ConfirmationPayload` / `DutyGuardConfirm`（§4 提交协议，请求侧 type 不含 duty_guard）；④ `MissingField.section` 注释订正为 0~10（基础信息字段会进点名清单）。同时订正本表 TK-03 行「API 契约 §3」→「§2」笔误（修订记录 #5 已登记）；《API 契约》「14 项 vs 13 项」仍待订正（权威文档改动，待确认后另补其修订记录）。必填/条件必填元数据（液氧 8 项 DATA-01、status=bad→note 必填、锅炉停机置灰）未纳入字典，留待 TK-06 实现表单时定载体（字典扩展或前端规则表）。
7. **v0.2 状态回写（2026-09-02）**：TK-03 review 补充修正（承 #6）——#6 采纳的「`fieldAnchor` 参数收窄为 `RecordFieldName`」有连带缺陷：电梯核对行在 records 无对应列（明细落 elevator_checks 逐台一行），而 ELE-04-T2 / ELE-07-T1 判据要求「拦截并点名」，收窄后 `MissingField.field` 无法表达电梯对象，C-09 的点名+跳转在电梯场景失效。正确修法是**放宽点名对象而非收窄锚点**：`errors.ts` 新增 `MissingTarget`（= RecordFieldName 或 `elevator:{id}` 模板字面量类型，仍拦拼写错误），`MissingField.field` 与 `fieldAnchor` 参数改用之，并新增 `toElevatorMissingField(elevatorId, label)`；`sections.ts` 新增 `ELEVATOR_SECTION_NO = 9` 免魔法数字。权威文档联动：《API 契约》§2 补明 `field` 取值域、`section` 含 0（基础信息）与 9（电梯）、`anchor` 生成式（`_` 与 `:` 均转 `-`），错误码表 ELEVATOR_EXPLANATION_REQUIRED 行补注点名形态，并追加其修订记录条目 2；**#5、#6 两次登记的「错误码表 14 项 vs 实为 13 项」笔误至此闭环**（按「修订记录只追加不删除」保留原条目文字，以新条目订正）。验证：`pnpm build` 三端全绿、`pnpm lint` 通过、`check-ledger.mjs` 全部通过。
8. **v0.2 状态回写（2026-09-02）**：TK-04 完成（⬜→✅）——认证与账号落地：契约 §3.1 三条路由（`POST /api/v1/auth/login`、`POST …/logout`、`GET …/me`）+ `SessionGuard`（D-T13 双通道：先 Bearer 后 Cookie，命中同一存根）+ `RolesGuard`（`@Roles`，C-05）+ 全局 `ApiErrorFilter`（所有异常统一为契约 §2 结构，C-09 基础设施）+ `DbModule`（全局单例连接池，关闭时结束池以保 Jest 退出）+ `app.setup.ts`（main 与测试共用前缀/Cookie 配置，防漂移）。会话存根落 `sessions` 表（第 13 张，**只存 SHA-256 摘要**），登录成功/失败均写 audit_logs（action=login，记 IP 与 User-Agent）；账号不存在与密码错误对外文案一致（F1-11-T2），已停用账号提示停用（F1-11-T3）。**判据：F1-11-T1/T2/T3 全绿** + 4 条 D-T13 验收（Bearer 通道、登出即失效、未登录 401、停用即吊销存根），`pnpm test` 9 项通过；`pnpm build` 三端、`pnpm lint`、`format:check` 全绿。前置定案（本轮同步完成的权威文档联动）：决策记录 **D-T13**（原拟编号 D-T12 已被「部署三容器」占用，按只增不复用纪律改用 D-T13）、技术方案 §4.1/§4.2/§6 + 修订 15、API 契约 §1/§3.1 + 修订 3、开发种子数据 configs 增 `session_timeout_minutes`（17→18，❓ 待科长确认）。工程侧：CI 增 MySQL service + `db:setup` 步骤（接口测试需真实库）；eslint/prettier 忽略 `.qoder/` 与 `drizzle/meta/`（工具产物）；兼修 `schema.ts` 中 **19 处 U+FFFD 损坏注释**（TK-02 遗留，已逐行对照 §4.2 原文重建）。队列下一个任务：TK-05（今日交接首页，依赖 TK-04 已就绪）。
9. **v0.2 修订（2026-09-03）**：TK-04 代码 review 后修正（承 #8）——① 审计来源 IP 改为受控采信 X-Forwarded-For：新增环境变量 `TRUST_PROXY`（.env.example 已补），为 true 时按 1 跳信任前置反代（生产 Nginx，Express req.ip 解析真实客户端地址），直连/开发不开启，替换原先无条件信任 XFF 头的写法（防伪造头污染审计 IP，C-05）；② 登录路径重复查 `configs.session_timeout_minutes` 去重：`AuthService.login` 一并返回 timeoutMinutes 供 Cookie maxAge 复用，避免两处取值因配置变更漂移。评审补漏两项：TK-22 由「三件」扩为「四件」（增 sessions 过期存根行清理——技术方案 §6 定时任务口径原拆解遗漏）；`login` 的 `channel` 非法值回落 `cookie` 的宽松口径记入决策记录 v1.1 增补。验证：`pnpm --filter @handover/api test` 9/9（真实 MySQL）、`pnpm lint` 通过。
10. **v0.2 状态回写（2026-09-03）**：TK-05 完成（⬜→✅）——今日交接首页三处落地。**后端**：新增 `apps/api/src/records` 模块，实现契约 §3.2 `GET /api/v1/records/today`（SessionGuard + RolesGuard + `@Roles('master','chief')`）：按 C-08 班次口径取当日记录（**只读，无记录时返回 `record: null` 而不建 draft 行**，D-T15）、板块填写状态与角标由 shared 字典派生、待同步标记为占位 `false`（离线队列在客户端、服务器零感知，F1-07-T2；TK-08/TK-15 接管）。**shared**：新增 `cards.ts`（12 张任务卡字典 + 卡片→字段映射 + 应填分母口径，`CARD_COUNT: 12` 与 `CARD_KEYS_MATCH` 两道编译期锁定）与 `dto.ts`（契约响应类型上移，api 产出与 h5 消费同一份，杜绝两端各写 interface 而漂移）。**前端**：h5 从脚手架页换为真实首页——登录页（Cookie 通道，前端不持有令牌）+ 今日交接首页（顶部进度条 + 12 张卡片 + 三态角标：异常红/已填绿/待填灰）+ 板块页最小版（只渲染本卡字段，F1-02-T2）；**不引入 vue-router**（两级视图用组件状态切换，路由方案留待页面数上来后统一定）。**判据：F1-01-T1、F1-02-T1/T2、F1-03-T1 全绿**——接口层 `records.spec.ts` 24 项（含 C-08 跨天六组时刻：北京 02:00/08:29/08:30/10:00/23:59/次日 00:00，对应台账 C-08 验收方式）+ UI 层 `tests/e2e/tests/today.spec.ts` 11 项（Playwright 真实链路：webServer 自动拉起 nest+vite，断言采"UI 与接口响应逐项对照"而非硬编码魔数）；`pnpm build` 三端、`pnpm lint`、`format:check`、`pnpm test` 33/33 全绿。**开工前钉死的口径与新发现的矛盾**：① 12 卡构成核自 spots 11 点位 + 液氧站拆两卡 = 10+2（非自己凑）；② 发现 **demo v0.3 的 12 卡按板块组织**（板块五合一卡、板块八独立一卡）与已定案的 **D-P17「按巡检点位/时段组织」**及 spots 表切分不同（总数同为 12），按 AGENTS.md「Demo 不是正式实现」以 spots 表为准，偏差**记入 TK-39**；③ 发现 `record_no` NOT NULL UNIQUE 与契约 §4「提交时生成 record_no」对"GET 建当日记录"构成硬矛盾，定案为只读（D-T15），同时**部分关闭技术方案 §11 第二项**（draft 行产生时机，写入部分仍待 TK-08）。**权威文档联动**：决策记录新增 D-P24/D-T14/D-T15/D-T16 + 修订 5；技术方案 §11 第二项部分明确 + 修订 16；台账 F1-02/F1-03「技术方案」列回填 + 待确认清单第 8 项 + 修订 7；《开发种子数据》configs 18→19（新增 `shift_start_time`，❓ 待科长确认）+ 修订 4。**附带修掉 TK-04 遗留低危项**：`auth.spec.ts` 的 afterAll 由全表重置 users.status 改为登记式只还原本测试动过的账号（liu/chief）——全表重置会掩盖其他测试或手工改动留下的状态；另两项低危（configs 缓存、DUMMY_HASH cost）仍按原计划留 TK-22 与安全加固。**工程侧**：api 的 jest 改为 `--runInBand` + `maxWorkers: 1`（两个 spec 共用同一真实 MySQL，并行会互删 sessions 与审计证据）；Playwright 配置增 webServer 与可选 `E2E_CHANNEL`（本地可复用系统 Chrome/Edge，CI 不设则用自带内核）；兼修 `auth.controller.ts` 一处 prettier 超长行（TK-04 遗留）。队列下一个任务：TK-06（板块填写页与校验，依赖 TK-05 已就绪）。
11. **v0.2 修订（2026-09-08）**：TK-05 判据核对时发现并修复一处**测试运行时刻依赖缺陷**——种子 D0 基准取自然日"今日"，而接口按 C-08 班次口径取 `duty_date`；凌晨窗口（当地时刻早于 `shift_start_time`）跑测试时 duty_date = 昨日 = 种子 D-1（有记录），「当日无记录」用例（F1-01-T1、F1-03-T1 初始态）必红（昨天 08:30 后跑全绿，属窗口性缺陷，评审发现）。修复：种子脚本的 D0 基准改按 C-08 班次口径，分界纯函数抽至 `apps/api/src/records/duty-date.ts` 供 seed 与 `resolveDutyDate` 同源共用（防两处漂移）；configs 种子 `shift_start_time` 值改引同一常量。《开发种子数据》§二/§六"今日"精确为「当前班次起始日」+ 修订 #5（语义本就如此，非口径变更）。验证：`pnpm --filter @handover/api test` 33/33（真实 MySQL）、E2E 12/12（`E2E_CHANNEL=chrome` 复用系统浏览器）、`pnpm build` 三端、`pnpm lint`、`check-ledger.mjs` 全绿。
12. **v0.2 状态回写（2026-09-09）**：TK-06 完成（⬜→✅）——板块填写页与校验落地。**shared（校验引擎，单一权威实现）**：新增 `validation.ts`——`validateFields`/`validateForError` 产出《API 契约》§2 同构错误体（code/missing_fields 逐条点名，缺失优先、越界其次）；数值范围分两层：**结构性约束即生效**（数值可解析且非负——负读数属录入错误）、**运营区间预留** `FIELD_RANGES`（❓ 待科长定稿后由 configs 下发，TK-27；预警区间 lo_press_range 是预警口径、不混作录入校验）。`cards.ts` 新增动态必填 `isRequiredField`、实时角标 `computeCardBadge`、`isFilledValue`（与 api 原实现同口径，三端共用）。**修复 TK-05 遗留口径冲突**：`t2_c830/t2_p830/t2_c2030/t2_p2030` 原按「仅 tank_in_use=2 时必填」条件化，与台账 **DATA-01「两罐两时点 8 项均必填（含 1 号罐压力）」**（PRD v0.2.5 修订 3）冲突——归位为恒必填，应填分母哨兵 **38→42**（DATA-03/04「在用罐枚举驱动」驱动的是用量取数与卡片标题，非必填口径）。**api**：`records.service` 的 required/filled 切动态口径（与 h5 同一函数，杜绝两端各算一套）；新增 `records-validation.spec.ts` 契约结构用例。**h5**：`SectionView` 升级为可编辑表单（数字键盘 F1-04、状态/枚举/多选/文本控件、单位全程标注、auto 派生列只读展示）；「完成本卡」本地校验 → **C-09 点名面板 + 点击跳转定位**（DOM id 与点名锚点同用 `fieldAnchor` 生成，两端必然对齐）；新增 `store/draft.ts` 会话内草稿（**持久化留 TK-08 在线草稿 / TK-15 离线队列**，刷新丢失为已知边界）；`TodayView`/`TaskCard` 角标改客户端实时计算（shared 同一函数对「草稿?? 服务端」合并取值重算，无草稿时与接口响应一致）。**层级口径说明**：F1-08-T2、DATA-01-T1 在《测试用例清单》标「接口层」，但提交端点属 TK-12；本任务以**契约结构级断言**先行锁死校验行为（`records-validation.spec.ts`，TK-12 复用同一引擎），TK-12 端点落地时以 supertest 对同组场景复验，届时层级与清单对齐。**判据：F1-08-T1/T3（E2E `section-fill.spec.ts`，含 DATA-01 的 2 号罐必填点名与 F1-04 数字键盘旁证）、F1-08-T2/DATA-01-T1（契约结构级）全绿**。验证：`pnpm build` 三端、`pnpm lint`、`format:check`、`pnpm test` 42/42（真实 MySQL）、E2E 17/17（`E2E_CHANNEL=chrome`）。兼修：`auth.spec.ts` F1-11-T1 审计断言取 audit_logs 首条被 E2E 真实登录历史行污染（本轮暴露），改按 id 倒序取本人最新一条。队列下一个任务：TK-07（上一班读数带出，依赖 TK-06 已就绪）。
13. **v0.2 修订（2026-09-10）**：TK-06 实现评审跟进（承 #12）——① **TK-12 判据补 F1-08-T2、DATA-01-T1**：两条用例在《测试用例清单》为接口层、语义为「提交 → 拦截并点名」，此前判据漏列致复验无主（结构级断言已由 `records-validation.spec.ts` 先行锁死，端点落地后以 supertest 复验，届时层级与清单对齐）；② **TK-08/TK-12 各补一条评审遗留关注项**（草稿 tombstone 语义收敛 `resetSession` 单一入口 / parseNumeric 拒绝 0x 十六进制形态与跨卡锚点唯一性），防评审发现的问题在后续任务中静默丢失；③ **代码层修复三项（M1/遗漏一/M4）**：h5 多选控件 `as string[]` 断言运行时崩溃改 `Array.isArray` 兜底（`SectionView.multiModelOf`，draft 空数组归一为清键）、401 被动掉线补清草稿并收敛 `App.vue resetSession` 单一入口（主动登出与被动掉线同路，防换账号串值穿透 C-05）、shared 新增 `FIELD_PRECISION` 精度抄录表（抄录技术方案 §4.2，零依赖约束下的折中，含缺失自检 `NUMERIC_FIELDS_MISSING_PRECISION` 与黄金值哨兵单测）；④ 「非选填即必填」推定原则在 `isRequiredField` 注释留痕，多选字段全不勾口径进台账待确认清单第 9 项；多选控件补 E2E 交互用例（此前 checkbox 零交互覆盖）。判据不变（新增用例为回归防护，不新增台账规格）。
14. **v0.2 修订（2026-09-10）**：TK-06 修复轮二次评审跟进（承 #13）——① **登录失败 401 与会话失效 401 区分**：`notify` 的清态仅对「已建立登录态后的被动掉线」生效（`user.value !== null` 守卫），登录请求自身的 401（F1-11-T2/T3 密码错/停用）不清草稿——TK-08 持久化草稿落地后，误清即输错一次密码丢本机草稿（违反 F1-09-T1 续填），TK-08 关注项批注同步订正；② **bootstrap 静默失败路径并入 resetSession 单一入口**（刷新后 Cookie 过期路径，为 TK-08 预落）；③ **测试命名挂台账编号**：多选交互 E2E describe 挂 `F1-08-T1`、精度哨兵比照分母哨兵先例收进 `records-validation.spec.ts` 的 `F1-08-T2` describe，消除非 `-T` 命名（铁律 6「需求即测试」；不新增清单行，节计数与 Phase 1 合计不变）；④ **M4 精度挂账 TK-12**：录入上限由 `FIELD_PRECISION` 派生（max = 10^(p−s) − 10^−s），提交校验时与 `FIELD_RANGES` 合流，本轮只挂账不接入；⑤ 工程卫生：`FIELD_PRECISION` 注释区分「DECIMAL 抄录 / INT 推导」、E2E 必填断言标 ❓ 依赖待确认第 9 项、`check-ledger.mjs` 补任务行列数校验并修复既有 TK-34～38 缺「依赖」列、台账状态行待确认清单计数同步至 9 项。
15. **v0.2 状态回写（2026-09-10）**：TK-07 完成（⬜→✅）——上一班读数带出落地。**shared**：`dto.ts` 新增契约 §3.2 `GET /records/today/prev` 响应类型（`PrevDto`/`PrevRecordDto`）；`cards.ts` 新增带出映射 `prevSourceField`（唯一跨时点映射：液氧 8:30 卡四项 830 字段取上一班记录的 2030 字段，DATA-02 判据「取昨日记录 20:30（非今日）」；其余取同名字段），三端同源。**api**：`records.service` 新增 `prev()`——按 C-08 班次日期取**紧邻前一条**记录（非「最近一条已提交」：前一条为 draft 时不跳班次回落更早记录，避免旧值冒充上一班，与 F1-05-T2「不显示脏数据」、F3-07「缺失→补录」同向）；两态区分：今日之前无任何记录 → `first_day: true`（F1-15 首班），前一条为 draft → `prev: null` 且非首班（F3-07 缺失态）；controller 新增 `GET /api/v1/records/today/prev`（`@Roles('master','chief')`，与 GET /records/today 同口径，契约角色列回写见契约修订记录 5）。**h5**：`SectionView` 进入本卡拉取带出，师傅亲手填/选的读数与状态字段逐项显示「上一班：值」（备注/派生列不显示），首班/缺失两态页顶提示（F1-15 文案「首班记录，无上一班数据可比对」）；拉取失败静默降级不阻塞填写（离线由 TK-15 接管）。**判据：F1-05-T1/T2、DATA-02-T1、F1-15-T1 全绿**（接口层 `records-prev.spec.ts` 10 项，黄金值钉死种子 k=9 读数、首班/缺失两态区分、不回落更早记录）；F3-07-T1 的「缺失 → 显'—'」半句以缺失态响应契约锁死，「补录入口 + 补录后计算正确」属提交链路与计算引擎（TK-12/TK-13），复验挂 **TK-14**（其判据本轮补列 F3-07-T1——该用例在《测试用例清单》归属 F3 节，而防呆三则含「上一班缺失显'—'允许补录」，承接 TK-06 评审 #13 的判据补列先例）。**测试口径说明**：种子日期相对灌种日，隔日未重灌时灌种日 D-1 不再是「今日班次 − 1」，测试基准取库内事实（duty_date 最大且小于今日班次的一行）而非日历推算；对库的临时改动（前一条置 draft、全体 duty_date +1000 天模拟首班）try/finally + afterAll 双重还原。验证：`pnpm --filter @handover/api test` 54/54（真实 MySQL）、`pnpm build` 三端、`pnpm lint`、`check-ledger.mjs` 全绿；UI 层比对值渲染与两态提示的 E2E 随 TK-31 全量回归覆盖。队列下一个任务：TK-08（草稿自动保存与续填；TK-09/TK-10/TK-11 依赖均已就绪，可并行取）。
16. **v0.2 修订（2026-09-11）**：TK-07 评审修复轮（独立评审发现 M1–M5 + 七项次要项，全部处置）——① **M1 取数口径缺陷定案**：原实现「取 duty_date < 今日的最新一条」在**漏交日**（相邻班次整日无行，F6-06 检测的真实场景）会回落更早记录冒充上一班（现场实证：种子隔日未重灌出现 2 班空档，接口返回前天读数而测试全绿），与已回写的「不回落」承诺及 F1-05-T2 判据冲突；按 **D-T17** 定案为相邻班次（今日班次日期 − 1 天）+ 非 draft 资格、缺失不回落（服务 `prev()` 重写，复用 `duty-date.ts minusOneDay` 勿另写日历逻辑），联动技术方案修订 17、契约订正 6、台账增补 #11；② **M2 同源断言修复**：「紧邻前一条」用例原用与实现同谓词查库算期望（回落多远都绿），改**相邻性哨兵**（`prevDate === minusOneDay(dutyDate)`，日历事实钉死，种子未当天重灌显式红）+ 新增**漏交场景回归用例**（相邻行临时平移 → 缺失态不回落）；③ **M3** 契约 §3.2 格子「无则返回 first_day:true」精确化为首班/缺失两态 + 修订 6；④ **M4** 新增决策记录 D-T17；⑤ **M5** 本行判据格加限定语（F3-07-T1 缺失态契约，补录与计算随 TK-14 复验）；⑥ 次要项：m3 类型收紧（`PrevRecordDto.readings` 改 `Partial<Record<RecordFieldName, unknown>>`、status 用 `RecordStatus`，去 as 断言）、m4 缺失态文案去掉未兑现的「可补录」承诺、m5 带出源日期显式展示（`prev-source`）、m6 台账 F1-05 格子恢复 §4.3 指针并列；**m1/m2 挂 TK-08 行**（prev 拉取 App 级缓存 + 401 并入 resetSession 单一入口）、**m7（带出显示 E2E）挂 TK-31**——TK-07 的 -T 用例在《测试用例清单》均为「接口」层级，判据已达，UI 断言随全量回归（与 TK-05 口径一致）。验证：重灌种子后 jest 全绿（含新哨兵与漏交用例）、build 三端、lint、format:check、check-ledger 全绿。
17. **v0.2 状态回写（2026-09-11）**：TK-08 完成（⬜→✅）——草稿自动保存与续填落地。**shared 无形状变更；api 无新路由**：按任务队列与技术方案 §5.1（D-T10 离线优先）定案草稿层为客户端 IndexedDB，服务端不设在线草稿端点——技术方案 §11 第二项「服务端 draft 行的产生时机」就此闭环（新决策记录 **D-T18**：records 行提交时一次性创建、draft 状态仅由撤回 TK-21 产生；D-T15 的「GET 只读」口径不变），契约 §3.2 GET/PUT /records/today/draft 两行订正为暂缓（订正 7）。**h5**：新增 `store/draft-db.ts`（IndexedDB 薄封装，存储不可用时静默降级为会话内草稿）+ `store/draft.ts` 升级两层结构——改动约 2 秒防抖自动保存、pagehide 冲盘、建立登录态后按 `draft:{user_id}:{duty_date}` 恢复并提示（PRD §6.1「草稿恢复提示」，SectionView 增「草稿已自动保存」指示兼 E2E 同步点）；**tombstone 语义按批注落码**：持久草稿清除只随本人登出/本人会话失效（App.vue `resetSession` 单一入口调 `clearAll`，内存与持久一并清），登录请求自身的 401（F1-11-T2/T3）不清本机草稿，bootstrap 静默失败路径因 currentKey 未建立无从归属、不动持久层（键隔离保证无串值，C-05）。**评审遗留 m1/m2（TK-07）一并处置**：/prev 拉取提升 App 级按 duty_date 缓存（App.vue `loadPrev`，同班次只拉一次）、其 401 经新增 `handleSessionLoss` 并入 resetSession 单一入口（不再静默吞错），SectionView 改 prop 消费。**判据：F1-09-T1 全绿**（E2E `draft-persist.spec.ts` 4 项：关闭重开恢复含恢复提示、登录 401 不清草稿、本人登出清除、prev 缓存旁证挂 F1-05-T1 层级先例——同「数字键盘旁证挂 F1-08-T3」先例，不新增清单行）；验证：`pnpm build` 三端、`pnpm lint`、`format:check`、`pnpm test` 55/55（真实 MySQL，重灌种子后相邻性哨兵复位）、E2E 22/22（`E2E_CHANNEL=chrome`）。队列下一个任务：TK-09（液氧板块专项）或 TK-10/TK-11，依赖均已就绪，可并行取。
18. **v0.2 修订（2026-09-11）**：TK-08 独立评审修复轮——① **M1 清除口径裁决（D-T18 修订 #9）**：持久草稿清除时点改定「该班次出现已提交记录」，登出/会话失效仅清内存（E2E 探针实证原口径在滑动超时后清掉整班已落盘草稿，违反 PRD §7 双保险与 EVT-06 保留条款）；`resetSession` 改调 `clearMemory`、新增 `markSubmitted`；E2E「本人登出清除」断言反转为「登出/被动 401 后草稿幸存」；② **M2**：TK-12 行补批注（提交成功须清本机持久草稿，否则重开复活且优先于服务端真值）；③ **M3**：IndexedDB 写操作以事务提交为准（指示器不早于落盘点亮）、防抖 2s→1s + visibilitychange 冲盘（pagehide 为 best-effort，注释钉明主防线）；④ **M4**：F1-09-T1 扩展断言（刷新路径、跨卡内容、hvac 数组/tank 枚举保真、填写期间零 `/records/today/draft` 请求）；⑤ 次要项：指示器写入即熄灭（m1）、`records.spec.ts` 残留注释订正（m5）、SectionView 模板统一 `prevInfo` 引用（m6）、draft-db 头注钉 `DB_VERSION` 升版与 EVT-06 时间戳挂账（m3/m8）、TK-15 行标注草稿层已落地（m4）、埋点清单 EVT-06 补实现状态批注。验证：build 三端、lint、format:check、jest 55/55（真实 MySQL）、E2E 24/24（`E2E_CHANNEL=chrome`）、check-ledger 全绿。
19. **v0.2 状态回写（2026-09-11）**：TK-09 完成（⬜→✅）——液氧板块专项落地（DATA-01 的 8 项恒必填已由 TK-06 归位，本任务余三块）。**shared**：新增 `calc.ts`（用量计算与时刻派生纯函数，TK-13 计算引擎的先头模块）——`loDayUseOf`（DATA-04：在用罐 `c830 − c2030`，在用罐缺读数**不回落备用罐**，浮点尾差按 (8,2) 列四舍五入，补液负值不夹逼）与 `localMeasuredAt`（DATA-13：`YYYY-MM-DD HH:mm:ss` 本地时间戳，与 MySQL DATETIME 字面量同形，离线时本机时钟即唯一权威来源）；`cards.ts` 新增罐号助手 `tankInUseOf/tankRoleOf/tankNoOfField`（DATA-03 标题联动的单一权威实现）与测量时刻映射 `MEASURED_AT_TARGET/measuredAtTarget`（830→am、2030→pm，与卡片时点切分同源）。**h5**：`SectionView` 写液氧读数时随读数把本机时刻写入草稿（清空读数不回收，保留已发生的真实测量；时刻随 IndexedDB 草稿持久化即离线本地时间戳），两罐读数行标签动态「（在用）/（备用）」（对齐 demo v0.3 验收形态，C-09 点名 label 仍取字典原值）；首页 `TaskCard` 液氧两卡标题动态后缀「N号在用」（台账「卡片标题」字面口径，未选罐号无后缀，today.spec 静态断言不受影响）。**层级口径说明（比照 TK-06 先例）**：DATA-13-T1/T2 清单标「接口」，提交端点属 TK-12——本任务先行锁定**客户端半边**（写值钉时刻 + 刷新幸存），**服务端「原样落库、不得以同步/接收时刻覆盖」挂 TK-12 supertest 复验**（TK-12 行批注与判据已同步补列；契约 §4 补充口径 + 修订 8，台账增补 #14）。**判据：DATA-03-T1（E2E `lo-tank.spec.ts`，含未选无后缀、8:30 选后 20:30 同步跟随、切罐实时反转）、DATA-04-T1（单元 `records-lo.spec.ts`，黄金值 4.3/6.0 钉死取数混淆即红 + 映射/格式哨兵）、DATA-13-T1/T2（客户端半边：E2E 两时点各自钉时刻、刷新幸存、清空不回收）全绿**。验证：`pnpm build` 三端、`pnpm --filter @handover/api test` 66/66（真实 MySQL）、`pnpm lint`、`format:check`、E2E 27/27（`E2E_CHANNEL=chrome`）。队列下一个任务：TK-10（锅炉停机联动）或 TK-11（新风多选），依赖均已就绪可并行取；TK-12（在线提交与预览）亦已就绪。
20. **v0.2 状态回写（2026-09-11）**：TK-10 完成（⬜→✅）——锅炉停机联动落地（DATA-05）。**shared**：`cards.ts` 导出 `REQUIRED_WHEN_BOILER_RUN`（锅炉运行三项单一集合）并新增 `isDisabledField`（boiler_run ≠ 'run' 含未选 → 置灰；**必填口径规则 4 与置灰交互同门**，不另维护清单）。**h5**：SectionView 三行置灰禁填（van-field / van-radio-group disabled）+「停机不填」标记 + `data-field-disabled` 供 E2E 断言；writeValue 选「停机」即清空三项停机列（先填后改停机的残留读数不清会以草稿值随提交上送，违反「置灰不填」；清键与 FieldValueGetter「未填→null」口径一致，残留报错高亮随清列熄灭）。**层级口径（比照 TK-06/TK-09 先例）**：DATA-05-T1 清单标「接口」，提交端点属 TK-12——api 以契约结构级断言先行锁定（records-boiler.spec.ts 6 项：停机整卡放行、残留值仍放行（api 不做「停机必须为空」反向校验，规格无此要求不擅加）、运行转必填逐条点名、置灰同门边界（含未选、cool_run 不联动）、动态分母哨兵停机 2/运行 5），**TK-12 supertest 复验挂账**（台账增补 #15 同步）；客户端半边 E2E boiler-stop.spec.ts 2 项（置灰+残留清空+停机放行回首页、反向联动转必填点名）。验证：pnpm build 三端、pnpm --filter @handover/api test 72/72（真实 MySQL，重灌种子）、pnpm lint、format:check、E2E 29/29（E2E_CHANNEL=chrome）。队列下一个任务：TK-11（新风多选）；TK-12（在线提交与预览）亦已就绪。
21. **v0.2 修订（2026-09-12）**：TK-10 独立评审修复轮——① **M1 判据归属收口**：「停机列留空」原仅由客户端清列单方面保证（records-boiler.spec.ts 第 2 项还把「api 不做反向校验」的立场钉死，同源盲区）——TK-12 行批注补「**submit 时 boiler_run='stop' → 三项强制写 NULL**」（客户端清列第一道 + 服务端落库第二道），测试第 2 项改口径分层措辞（校验层残留放行 ≠ 落库留空），断言本身不变；② **M2 demo 依据订正**：demo v0.3 的默认置灰来自脚本预置 stop（从未出现未选态），不能为「未选即置灰」背书——cards.ts `isDisabledField` 注释改保守口径说明，h5 标记文案「停机不填」→「**选运行后填**」（未选时不再替师傅宣布停机；E2E 文案断言同步）；③ **m3 C-09 清单与实际一致**：改停机清列后，已展开的报错面板按清列后状态整体重算（同一校验引擎），不再依赖二次点击，E2E 断言改为「改停机后面板即收起」；④ **m4 E2E 真禁用断言**：补 `.van-radio--disabled` 计数 + 强点禁用项无选中态（`aria-checked="true"` 计数 0），不再只断言自维护的 `data-field-disabled`。验证：`pnpm build` 三端、`pnpm lint`、`format:check`、`pnpm --filter @handover/api test` 72/72（重灌种子）、check-ledger 全绿；E2E 本轮全量三跑均为 28/29，唯一红点均落在 `draft-persist.spec.ts`（与本轮改动无关的水卡草稿恢复路径，且三轮红点不全相同；含新增 m4 断言的 boiler-stop 两条三轮全绿；该文件单独复跑与合跑均 8/8 绿）——判定为 TK-08 时期已记录过的 4 workers 并行竞速 flake 族（当时「复跑即绿，未挂账」），本轮负载下出现频率升高，是否需收敛并行度/防抖窗口待后续任务观察，不在 TK-10 范围内擅改；台账增补 #16 同步（增补 #15 的 demo 依据以 #16 为准，原文保留）。
22. **v0.2 状态回写（2026-09-12）**：TK-12 完成（⬜→✅）——在线提交与预览落地。**shared**：`validation.ts` parseNumeric 收紧为十进制字面量（TK-06 评审 M5）+ `numericMaxOf`（FIELD_PRECISION 派生录入上限，与 FIELD_RANGES 运营区间合流，M4）；`dto.ts` 新增 `SubmitPayloadDto`/`PreviewDto`/`SubmitResultDto`（契约 §4 形状，confirmations/duty_guard_confirm 本阶段仅接收不判定）。**api**：`POST /records/today/preview`（F1-10 未填/异常两张点名清单，接班人修改原因条件预检一并点名）与 `POST /records/today/submit`（同事务：C-09 校验 400 → record_no=`HB-YYYYMMDD-001`、status=submitted、submitted_at=服务端收到时刻 → 审计 record.submit；枚举列越值拦为 400 防 MySQL 500、hvac_locs 数组原样落库、lo_measured 原样落库与 submitted_at 分源、停机三项强制 NULL、接班人=次日排班带出（duty-date.ts 新增 plusOneDay）+ 修改必填原因留痕；payload 基础信息列（duty_date/submitted_at/submitter_id）一律不采信）+ GET /records/today 回显 receiver（records.spec.ts 对应断言同步按带出口径回改）。**h5**：首页提交入口 → 预览弹窗（未填项/异常项点名、清单项点击经 shared CARD_BY_FIELD 跨卡跳转定位、未清零前确认提交置灰）→ 提交成功 toast 含交接单号 + **draft.markSubmitted**（D-T18 修订 #9 挂账钩子）；服务端 400 点名清单回填弹窗（C-09 不静默吞错）；修改接班人的人员候选 UI 挂 TK-26（h5 无师傅端人员清单接口，本阶段仅展示带出值）。**测试**：新增 `records-submit.spec.ts` 23 项（F1-10-T1、F2-01-T1、F1-08-T2 含收紧与上限、DATA-01-T1 八项遍历、DATA-05-T1 双向、DATA-07-T1、DATA-09-T1、DATA-10-T1 两态、DATA-13-T1/T2、RECORD_EXISTS、403；提交成功类用例用例内即删自建记录避让 duty_date 唯一），jest 全量 102/102（重灌种子）；E2E 新增 `submit-preview.spec.ts` 3 项（预览点名与跨卡跳转、提交成功流程 + markSubmitted 草稿清除、400 点名回填）——提交成功路径以 route 拦截回 201 验证客户端半边（真实写库会与并行 spec 的 today.spec「当日无记录」断言竞争，全链路 E2E 随 TK-31），全量 33/33（E2E_CHANNEL=chrome；全量首跑出现一例 draft-persist 水卡断言红点，单跑与复跑均绿，与 TK-10 修订 #21 记录的并行竞速 flake 族同征）。**文档联动**：契约订正 11、台账增补 #19（F1-08/F1-10/F2-01/DATA-05/07/09/10/13 八条技术方案列回填）。**补记**：TK-11（新风多选）2026-09-12 完成（⬜→✅）时本修订记录漏一条状态回写条目，落地详情见台账增补 #17/#18 与契约订正 9/10，本条一并补录。**挂账移交**：防呆 409（need_confirm/confirmations）→ TK-14、duty_guard 安全阀判定 → TK-26、用量列固化 → TK-13（TK-12 阶段提交置 NULL，契约 §4 第 3 步不信任客户端传值）、alerts 标红 → TK-17/TK-22。验证：pnpm build 三端、pnpm lint、format:check、check-ledger 全绿。队列下一个任务：TK-13（用量计算引擎）/TK-15（离线三层缓冲）依赖已就绪，TK-14 随 TK-13 后取。
23. **v0.2 修订（2026-09-12）**：TK-12 独立评审修复轮（评审发现 M1–M6 + L1–L7 + 文档 m 项，M 级全修、L 级择要；评审报告逐项探针实证）——① **M1 小数位校验**：shared validateFields 增列精度小数位校验（toFixed 往返比对，浮点边界安全；实证 100.05 入 (12,1) 被四舍五入、0.0000001 入 (5,2) 被抹零、b40 上送 10.7 被截断）——台账增补 #20 同步订正 #19「原样落库」表述；② **M2 文本列长度**：shared 新增 `FIELD_LENGTHS`（varchar 照录 §4.2 DDL）+ `TEXT_FIELDS_MISSING_LENGTH` 哨兵，超限 400 点名（hp_note 300 字 500→400），h5 textarea 同源绑定 maxlength；③ **M3 测量时刻**：分域捕获组正则 + 日历有效性（Date 往返），非法置 NULL 不落库（原 '2026-13-45 99:99:99' → 500）；④ **M4 接班人**：receiver_id 显式上送一律校验存在性（防无基线绕过留痕直撞 FK 500）+ **无带出基线显式指定视为修改、须原因**（用户拍板的保守口径，科长复核可放宽）；回归用例 try/finally 临时删 D+1 排班行制造无基线态；⑤ **M5 撤回重提快照**：api 更新路径对字典存储列未上送者一律写 NULL（SNAPSHOT_NULL_DEFAULTS）+ h5 buildPayload 改「草稿 ?? 服务端值」全字典合并视图——单用任一方会分别产生「重提清空整单」或「清空字段回落旧值」，两道防线互补；⑥ **M6/L1**：提交入口条件改「无记录或 draft 态且 master」（原 `!today.record` 使撤回重提无入口、科长巡查见 403 死按钮）；⑦ L 级：L2 markSubmitted 返回清除结果、失败显式提示；L3 hvac_locs 元素须 string；L4 preview 并入 normalize 越值清单（buildValidationError 缺失优先选码，纯越界仍报 OUT_OF_RANGE）；L5 recordNoOf 抽纯函数 + 黄金值哨兵钉 §4.2 示例（'2026-08-27'→'HB-20260827-001'）；L6 NUMERIC_MIN 行注释漂移订正 + previewLoading 死状态移除；L7 E2E 去硬编码 user id（改 /auth/me 实取）与固定等待。**判据**：F1-08-T2（含小数位/长度/多选元素）、DATA-10-T1（含无基线三态）、DATA-13-T1/T2（含非法时刻置 NULL）扩容复验全绿（records-submit.spec 23→35 项，jest 全量 114/114 重灌种子）；E2E 新增提交入口状态机/角色门控三条（route 拦 GET /records/today 不写库避让 today.spec——正是原提交路径全拦截导致 M6 溜过 E2E 的盲区补测），36/36。**文档联动**：台账增补 #20（含 F1-08 Demo 状态列 ⚠️→✅ 订正）、契约订正 12。门禁：pnpm build 三端、pnpm lint、format:check、check-ledger 全绿。
24. **v0.2 修订（2026-09-12）**：TK-13（用量计算引擎）完成（⬜→✅）——shared `calc.ts` 补齐水/电/气三类口径纯函数（`waterDayUseOf`/`eDayUseOf`/`gasDayUseOf` + `USAGE_FIELDS`/`usageScaleOf`/`roundToScaleOf`，与 TK-09 `loDayUseOf` 同模块同源）；api 提交链路接契约 §4 第 3 步（用量服务端计算固化、sections 里 `*_use` 传值不采信——诱饵传值用例锁死；上一班取数抽 `adjacentPrevRow` 与 /prev 同源，D-T17）；新增覆盖协议 `usage_overrides[]`（shared dto `UsageOverridePayload`；服务端 reason 强制 F3-06-T2、覆盖值按列精度取整固化、审计 `record.usage_override` 留痕含 old_value 自动值）；GET /records/today 增 `manual` 旗标（audit 反查，F3-06-T1）；h5 SectionView 用量实时预览（分线同屏）+ lo_day_use 覆盖入口与原因预检、App.vue buildPayload 组装覆盖清单、草稿层增 `usage_override_reason` 非字典键。**测试**：新增 `records-usage.spec.ts` 17 项（F3-01/02/03/04-T1 单元黄金值 + 接口层固化/覆盖/无原因拦截/越值越键；种子 D-1 读数基准哨兵钉死防同源盲区）；`records-submit.spec.ts` 的「用量列置 NULL」TK-12 时代断言同步订正为固化断言（黄金期望 −12150/−96220/−30/0）；jest 全量 131/131，E2E 36/36。**文档联动**：契约订正 13、台账增补 #21。**挂账移交**：防呆 409 与充气确认后单卡按 0 计取数层 → TK-14、alerts 标红 → TK-17/TK-22、duty_guard 安全阀 → TK-26、用量重算与豁免 → TK-16。验证：pnpm build 三端、pnpm lint、format、check-ledger 全绿。
25. **v0.2 修订（2026-09-12）**：TK-13 独立评审修复轮（评审报告 M1–M4 + L1–L6 全修，L2 宽口径由用户拍板）——**M1** manual 旗标按版本过滤（usageOverrideFieldsOf 增 version 参数；撤回重提回归用例钉死，TK-16 豁免判定依据随之钉死为当前版本审计行）；**M2** h5 覆盖判定 draft-only 统一（SectionView `overridden` 旗标，onComplete 预检与 buildPayload 同数据源，服务端已有值不再误当本次覆盖）；**M3** preview 与 submit 共用 `validateUsageOverrides`（覆盖校验预览即点名，L4 同纪律）；**M4** 覆盖原因超长以字段字典名义点名（不再误用「不支持的用量覆盖字段」）；**L1** 上一班取数与四类计算移到校验/409 判定之后；**L3** 计算值超 DECIMAL 列容量置 null 不入库（1264→500 回归用例）；**L4** 同字段重复覆盖项 400 点名；**L5/L6** submit 头注释漂移订正、h5 清覆盖值连带清原因；**L2 定案（宽口径）**：USAGE_FIELDS 四键均可覆盖（D-P08 字面依据），fill='auto' 语义=服务端自动计算固化+Phase 1 无 h5 覆盖入口，水/电/气覆盖入口挂账随记录详情/科长后台。测试：records-usage.spec 17→22（五条回归；测试日历推算改用生产 minusOneDay）；jest 136/136、E2E 36/36、build 三端、lint、format、check-ledger 全绿。**文档联动**：契约订正 14、台账增补 #22。TK-13 状态不变（✅）。
26. **v0.2 修订（2026-09-13）**：TK-14（防呆三则）完成（⬜→✅）——**判定层与计算层分层**：shared 新增 `guard.ts`（`decreasedReadingsOf`/`refillCardsOf` 纯函数；判定范围定案 **D-T19**：累计走字表三字段 water/e1/e2 入回退判定，液氧含量递减是常态不入判定，气卡剩余量增大单独走充气判定，D-P14 单卡判定）；api submit 契约 §4 第 2 步落地——409 `READINGS_DECREASED` / `GAS_REFILL_CONFIRMED` + need_confirm 可解释清单（含上一班/本次值；回退与充气同时命中时 READINGS_DECREASED 优先），确认消费口径：reason 空白视为未确认（F1-12-T2 不因重试放行）、与命中项对不上号的确认不消费、仅命中项的确认写审计（record.submit 逐命中项一行，契约 §5 精确化）；**充气确认取数层**：`gasDayUseOf` 增 `refilled` 参数，确认成立的卡按 0 计、另一卡正常（TK-13 挂账闭环），未确认时负差原样固化；**补录协议（D-T19）**：payload 增 `prev_readings`（白名单 shared `PREV_BACKFILL_FIELDS`，非法值 400 越界点名，校验与消费解耦），服务端仅**非首班缺失态**消费（有上一班记录时忽略，防缓存串台）并作为防呆比对与用量计算基线，补录值不落 records 列（缺失班次不建行，F6-06 不受影响）、审计 `record.prev_backfill` 留痕；h5：409 确认弹窗逐条收原因后重提（取消不清草稿可稍后重提）+ SectionView 缺失态补录入口（草稿键 `prev_backfill:*` 随草稿持久化即离线可用，用量预览同步回落补录值）。**测试**：新增 `records-fangdai.spec.ts` 13 项（判据 F1-12-T1/T2、F1-13-T1/T2、F3-07-T1 + 单元与回归；缺失态以相邻班次行临时置 draft 构造——即 D-T17 语义——try/finally 还原，不碰种子行外键）。**门禁**：build 三端、lint、format、check-ledger 全绿，jest 全量 149/149。**文档联动**：契约订正 15（§3.2 submit 行/§4 第 1、2、3 步/§5 审计表；顺带清除修订记录一条乱码损坏重复行）、台账增补 #23（F1-12/F1-13/F3-07 技术方案列回填，Demo 状态列不动）、决策记录增补 10（**D-T19**）。队列下一个任务：TK-15（离线三层缓冲，依赖 TK-12 已就绪）或 TK-16（下游重算，依赖 TK-13 已就绪）。
27. **v0.2 修订（2026-09-13）**：TK-14 独立评审修复轮（承修订 26；评审报告 M1–M2 + L1–L2 全部探针实证后修复）——**M1** 确认原因（confirmations[].reason）超 `audit_logs.reason` varchar(200) 时原实现直写审计列 → MySQL 严格模式 1406 → 事务回滚 500（探针实证：201 字 → 500、5 字 → 201），修复为 400 越界点名、以字段字典名义（reading_decreased 点该 field、gas_refill 点对应气卡字段，与用量覆盖原因超长 M4 同口径）+ preview 与 submit 消费同一 `validateConfirmations`（M3/L4「预检即点名」先例）；**M2** `confirmations` / `usage_overrides` 上送非数组 → for..of 迭代器异常 500（探针实证），修复为 400 合成定位项点名（域外回显同契约订正 2 ① 先例；undefined 未上送仍合法）；**L1** `prev_readings` 补录值补 `numericMaxOf` 列容量上限与非负下限（原 '-500' 等脏基线可进入 need_confirm 可解释文案与 record.prev_backfill 审计，探针实证）；**L2** TK-13 行「唯一非字典键」表述行内订正（本条与修订 26 为据，原文指向修订）。新增回归 5 项，`records-fangdai.spec` 13→18；jest 全量 154/154（当天重灌种子）、E2E 36/36（E2E_CHANNEL=chrome）。**文档联动**：契约订正 16、台账增补 #24。**运维前提留痕**：接口用例强依赖当天重灌种子——C-08 班次边界（08:30）跨过灌种日后 `duty_date` 前移，相邻班次落到刻意留空的 D0 → 相邻性哨兵显式红（本轮实测复现，`pnpm --filter @handover/api db:setup` 后复绿），与 D-T17 轮记录的同源盲区同族。
28. **v0.2 状态回写（2026-09-13）**：TK-15 完成（⬜→✅）——离线三层缓冲的**待同步队列**落地（草稿层 TK-08 已落地；照片暂存区随 TK-36）。**shared**：`guard.ts` 增 `unconfirmedNeedConfirmItems`（409 need_confirm 组装自 records.service 下沉 shared，api 提交侧同步改为消费该函数、判定与文案单一权威实现，行为不变，jest 154/154 无回归）。**h5**：① `store/draft-db.ts` DB_VERSION 1→2 增 `sync_queue` store 并补 `onblocked` 回调（兑现 TK-08 挂账），通用单 store 事务执行器 `idbWithStore` 导出；② 新增 `store/sync-queue.ts` 队列存取（入队/读/改/删 + `queued_at` 排序，写入前 JSON 深拷贝——**Vue 响应式 Proxy 经 structured clone 会 DataCloneError**，草稿层 snapshot 同款手法）；③ App.vue：离线提交链路（preview 网络失败 → 本地同口径预检带「当前离线」横幅；submit 网络失败 → shared guard 本地防呆预检确认前置 → 入本机队列 + 判据文案 toast）+ 排空引擎 `drainQueue`（queued_at 升序逐单上传；401 并入 handleSessionLoss、RECORD_EXISTS 自愈移除、业务拒绝固化 last_error 继续后续单、网络中断保持队列原状）+ 同步状态全程可见（TodayView `sync-chip` 本机队列 OR 合并接口 pending_sync + 队列横幅 + 「立即同步」）+ F1-14 强提醒（队列项 `duty_date` 早于当前班次 → 全屏不可忽略 overlay，有滞留项时自动排空被门控；online 监听门控前重读队列防内存镜像滞后）+ 队列项 payload 随草稿自动保存刷新（watch lastSavedAt，D-T20）。**测试**：新增 E2E `sync-queue.spec.ts` 4 项（F1-06-T1/F1-07-T1/T2/F1-14-T1；账号用 wang 避让 zhang 的并行 spec；断网用 `context.setOffline` 注入——**离线态下 Playwright route 拦截仍生效**，同步上传的 201 mock 必须在离线入队之后注册，否则离线提交会被拦截走在线成功路径；**离线下 logout 请求发不出去、服务端会话仍在**，切接班人视角前须 clearCookies，否则 bootstrap 残留 Cookie 自动恢复登录态触发自动排空真实写库）；E2E 全量 40/40（E2E_CHANNEL=chrome；首跑 draft-persist 一例并行竞速 flake，单跑复绿——TK-10 修订 #21 同族，未挂账不在本任务擅改）。**文档联动**：台账增补 #25（F1-06/07/14 技术方案列回填）、决策记录增补 11（**D-T20** 队列与草稿层边界）、技术方案 §5.1 实现落点段 + 修订 20、埋点清单 EVT-04/EVT-05 实现状态批注 + 修订 3。队列下一个任务：TK-16（下游重算与豁免，依赖 TK-13 已就绪）或 TK-17（电梯核对表单端，依赖 TK-06 已就绪）；M2 仅余此二项。

29. **v0.2 修订（2026-09-13）**：TK-15 独立评审修复轮（评审报告 M1–M6 + L1–L6 + m1–m4；M 级全修、L 级全修、m 项择要；M3/M4/M6 经一次性 E2E 探针实证后修复，探针跑完即删）——**M1 跨班次滞留单禁止上传**：服务端 submit 只认当前班次（C-08，payload 无 duty_date 且不采信），滞留单跨班次上传必然落成当天记录（D 日永久无行→F6-06 误报漏交、挤占当日提交位）；修复为排空引擎对非当前班次项固化「已过班次分界，需科长处理」并跳过，F1-14 overlay 处置动作改「联系科长」指引、师傅确认后收起（不可忽略式读法精确化见台账 F1-14 行）；**补交端点属契约变更，挂待拍板新决策（拟 D-T21），不随本轮静默实现**；F1-14-T1 断言按 M1 翻转（跨班次项零上传请求）。**M2** RECORD_EXISTS 自愈仅同班次语义下成立。**M3/M5**：草稿清除改统一冲账（已上传班次集合循环外逐班次 markSubmitted，登出/401 中途失效也不漏清），清除失败显式提示；today 未就绪不排空；会话代次 epoch 比对 + 非网络异常不得伪装网络失败静默吞掉（client.ts 新增 NetworkError 类型）。**M4**：同步在途锁开卡与新提交（openCard/onOpenPreview 守卫 + TodayView 提交钮 disabled），在途补改（已在卡内场景）完成后显式提示「未包含在交接单中，请核对」。**M6**：离线预检始终执行、确认清单入队即清——原「收过一次确认即整块跳过」会让异字段新命中带着旧确认入队、同步时刻 409 滞留（探针 P3 实证）；确认多轮按项合并（同键覆盖）。**L 级**：L1 补录读数校验下沉 shared（validatePrevBackfillReadings，api 与 h5 同源）并纳入离线预检；L2 业务拒绝达 5 次停止自动重试 + overlay 提示联系科长（处置面板挂 TK-24）；L3 手动同步离线时显式反馈；L4 IndexedDB onversionchange 主动放行 + 开库 8s 超时（升版阻塞不再永久挂起）；L5 登录页提示本机不分账号未同步单数（不含内容、不代传）；L6 队列补丁失败显式提示。**m 项**：m1 文档「EVT-04/05 触发点已落码」过度承诺订正（台账增补 #26 ⑦、埋点修订 4）；m2 同班次重提保留 attempts/last_error 诊断；m3 records.service 防呆命中双算留痕（行为等价，随 shared 消费收敛观察）；m4 排空末尾 loadToday 的自动排空因 syncing 在途被丢弃（语义如此，留痕防误改）。测试：sync-queue.spec 4→9 项（F1-14-T1 翻转 + M3/M5、M4、M6、L3、L5 五条修复回归）；门禁：build 三端、lint、format、jest 154/154（重灌种子）、E2E 全量 45/45、check-ledger 全绿。遗留：补交端点 + D-T21 待拍板（联动 TK-16 重算与 F6-06 语义）；L2 处置面板挂 TK-24。

30. **v0.2 状态回写（2026-09-13）**：TK-16 完成（⬜→✅）——下游重算与豁免落地，**D-T21 经用户拍板定案**（本任务开工前 AskUser 三问：① 本任务落地补交端点+重算；② 提交人恒为登录人本人；③ 仅重算紧邻下游 D+1），闭环修订 #29 遗留的待拍板悬案。**shared**：dto 新增 `BackfillPayloadDto`（extends SubmitPayloadDto + duty_date）与 `SubmitResultDto.recalc`（在线提交响应无此键）。**api**：records.service 提交链路重构为 `submitCore` 单一实现（在线提交与补交同口径复用，dutyDate 由调用方决定，防两套提交口径漂移）——`computeUsageValues` 改收 cur/prev 双 getter（提交侧读 payload、重算侧读库行）、`usageOverrideFieldsOf` 加事务执行器参数；新增 `backfill()`（duty_date 日历校验 + 严格早于当前班次，越界 400 点名 duty_date）与 `recalcDownstreamInTx`（同事务重算 D+1 已提交行的 water/e/gas 三项 prev 依赖用量：豁免 = 当前版本 usage_override 审计行 D-T07；值无变化不更新不写审计；充气确认延续经新助手 `confirmedRefillCardsOf` 从 record.submit 审计 type=gas_refill 行反查供 gasDayUseOf 的 refilled 参数；逐变更项写 `record.recalc`）+ `record.late_submit` 审计；controller 新增 `POST /records/backfill`（master,chief，契约订正 17）。**测试**：新增 `records-backfill.spec.ts` 6 项（F3-08-T1：补交 → 下游三项重算黄金值 500.0/2000.0/15.0 + recalc/late_submit 审计 + 补交自身用量缺失态固化 null；F3-08-T2：覆盖豁免 + 原因保留 + 零噪音审计；回归：duty_date 约束含 02-30 非法日历日、RECORD_EXISTS、防呆 409 同协议确认重提留痕；固定历史日期 2026-01-09/10/11 远离种子区，自建行无 record_versions 子行可安全删除）；jest 全量 160/160（重灌种子）。**文档联动**：契约订正 17（§3.2 新路由、§4 补交口径、§5 审计两行，路由 33→34）、技术方案修订 22（§4.3 实现落点段）、台账增补 #27（F3-08 列回填）、决策记录增补 13（D-T21 行 + 修订 #12 悬案闭环）。**遗留挂账**：h5 排空引擎滞留单处置入口接入补交端点（「联系科长」→ 自助补交）挂 TK-24/后续；科长代录他人挂 TK-24；异议修改触发重算挂 TK-20（复用 recalcDownstream）。门禁：build 三端、lint、format、check-ledger 全绿；E2E 随门禁复跑。队列下一个任务：TK-17（电梯核对表单端，依赖 TK-06 已就绪），M2 仅余此一项。

31. **v0.2 修订（2026-09-14）**：TK-16 **独立评审修复轮**（评审报告 M1 + L1–L9 + m1–m5；逐条代码/DB 探针实证后处置，**M1 探针用例先跑红再修转绿**，并由一次性探针转为正式回归用例）——① **M1 数值判等**：重算的「值是否变化」原按字符串比较，而计算侧 `String(roundToScaleOf(..))` 在整数场景产 '500'、DECIMAL(12,1) 列读回 '500.0'，**恒不相等**→ 整数用量总被误判为变更，产生假 UPDATE + 假 `record.recalc` 审计，使契约 §5/技术方案 §4.3/D-T21 三处「值无变化不写审计」承诺静默失效（且**今天即可触发**：师傅补录值与后来晚到的实际读数相同时）；修为新增 `sameUsageValue`（null 与非 null 视为不同）。② **L1**：无变更且无待复核项时 `recalc` 返回 null（原返回 `{fields: []}` 与 DTO/契约注释不符）。③ **L2 状态门控（拍板）**：`recalcDownstreamInTx` 只重算 `status='submitted'`，objection/completed 跳过并留日志（原仅排除 draft，**已双方签名归档的单据可被一次补交静默改数字**，与 §5.4/DEP-08 相逆）。④ **L3 补交窗口（拍板）**：`duty-date.ts` 新增 `minusDays` 与 `DEFAULT_BACKFILL_WINDOW_DAYS=7`，service 新增 `backfillWindowDays()` 读 configs `backfill_window_days`（限幅 1–365，非法回落常量），duty_date 早于窗口即 400 点名；**种子新增该键（configs 19 → 20）**、台账待确认清单新增第 10 项。⑤ **L5 draft 接管（拍板）**：补交改为仅对非 draft 行 409（与 submit 同一条件），draft 行走更新+version+1（消除「提交→撤回→离院未重提」的永久死角）。⑥ **L6 待复核清单（拍板）**：dto 新增 `RecalcResultDto`（`fields` 收窄为 `readonly UsageFieldName[]` + 新字段 `needs_review: ConfirmItem[]`），重算时用 shared `unconfirmedNeedConfirmItems` 判定（已确认充气卡不重复计入、历史回退确认不沿用 D-T20 M6），命中则不改数不拦提交而写审计 `record.recalc_review`。⑦ **m5**：`usageOverrideFieldsOf`/`confirmedRefillCardsOf` 去掉 `executor = this.db` 默认值（防事务内漏传退化为静默旧读）。⑧ **m1/m2 文档订正**：契约 §4 补交口径「当日/未来班次走**本路由**」错字（应为 /today/submit）；台账 #27 自称「技术方案列回填」实际改了出处列，本轮已把落点止于**「技术方案」列**（列序：编号|规格要求|出处|阶段|技术方案|Demo 状态|验收方式）；§4.2 `audit_logs.action` 注释补登记三个新取值（m4）。⑨ **不成立的评审项（已核实，未改）**：事务内死锁（仅 D→D+1 单向加锁，其余读在事务外）、executor 漏传（本轮调用处均显式传 tx）、单层 D+1 推理、黄金值同源盲区、manual 旗标污染。**本轮主动未做（已文档挂账）**：`/records/backfill/preview`（无消费者，随 TK-24）、补交班次归属校验（TK-26）、行锁与 1062→409（TK-20 复用前必补）；`usageLiteralOf` 形态统一（评审建议的修法②）也未做——数值判等已消除行为差异，改形态会级联影响现有审计断言。测试：records-backfill.spec **6→13 项**，且测试日期由固定历史日（2026-01-09/10/11）改为**相对当前班次动态计算**（D-17/D-16/D-15）——固定日配窗口限幅 365 会随日历推移失效（本轮实测：设 400 被回落成 7 导致整片 400）。门禁：build 三端、lint、format、jest 全量 **167/167**（重灌种子）、check-ledger 全绿。**重要教训（本轮两次发生）**：`SearchReplace` 报「Failed to match」时仍可能**已部分应用**并吞掉整段 import / 误删代码行；另 `git checkout -- <file>` 回退越界改动时连带回退了本任务的正当改动（dto 的 BackfillPayloadDto），而 jest 因引用 `packages/shared/dist` 旧产物而**仍然全绿**（假绿），靠 tsc/build 才发现——回退单文件前须确认该文件内包含哪些本任务变更，且 shared 改 src 后必须重新 build 再跑测试。联动：契约订正 18、技术方案修订 23、台账增补 #28、决策记录 D-T21 修订（增补 14）、《开发种子数据》修订 6。TK-16 状态不变（✅）。队列下一个任务：TK-17（电梯核对表单端，依赖 TK-06 已就绪），M2 仅余此一项。

32. **v0.2 修订（2026-09-14）**：TK-16 修复轮**红队对抗评审**（针对修订 31 的修复本身，六攻击面：L6 判定语义/L5 接管/L3 窗口旁路/M1 判等形式/测试质量/六处文档一致性）——结论：**无 M 级缺陷，修复轮保持 ✅**，六处文档表述互相一致。处置 2 L + 2 m：① **L1 确认复用不对称（探针实证后修）**：重算的 needs_review 原对充气确认按卡号反查复用（confirmedRefillCardsOf）、对回退确认传空集——同值命中（补录值恰与晚到实值一致）被重复标出（探针用例先红后绿）；但**不采纳评审建议的字段级复用**——重算会改变基线，字段级复用会让「新基线下更大的回退」被旧确认静默解锁（D-T20 M6 同族陷阱，比噪音更危险），定案为**值匹配复用**（field+prev+current 三元组一致才消音）：shared guard 新增 `decreaseHitKey` 与 `needConfirmItemsExcludingHits`（清单组装抽取私有 `needConfirmItemsOf` 单一来源，原 `unconfirmedNeedConfirmItems` 行为不变）、service 新增 `confirmedDecreaseHitsOf`；新增防过度消音守卫用例（异值命中仍标出）。② **L2 收尾**：draft 接管的 `record.late_submit` 审计 newValue 补记 `prev_submitter_id`（归属变更 A→B 不靠 actor 间接推断；归属硬校验维持挂账 TK-26）。③ **m1/m2**：`sameUsageValue` 冗余三元简化为 `===`；`backfill()` 窗口值单次读取（原校验与文案各读一次存在文案竞态）。红队核实不成立的项（原文留痕）：判定用原始读数（D-T19 即判读数）、液氧误报、豁免×needs_review 正交、draft 接管无快照不违 §5.4（record_versions 仅异议重提写入，撤回段只要求审计+版本）、窗口含端点语义一致、DECIMAL 负数/零形态不可达、setWindow 残留无消费面。测试：records-backfill.spec **13→15 项**（同值命中消音 + 异值命中仍标 + L5 接管留痕断言）；门禁：build 三端、lint、format、jest 全量 **169/169**（重灌种子）、E2E 全量、check-ledger 全绿。联动：契约订正 19、技术方案修订 24、台账增补 #29、决策记录增补 15。
33. **v0.2 状态回写（2026-09-14）**：TK-17 完成（⬜→✅）——电梯核对（表单端）落地。**shared**：新增 `elevator.ts`（三端同源纯函数与校验：`expectedStatusAt` 预期计算 [start,end)/跨零点/多窗口/脏配置回落 run、`parseWindowsOf`/`clockMinutesOf`/`normalizeActualOf`/`isMismatchOf`、`validateElevatorChecks` 纯校验——api 提交/预览与 h5 卡内/离线预检消费同一实现，缺失类点名复用 TK-03 备好的 `toElevatorMissingField` elevator:{id} 形态）；`calc.ts` 上移 `isValidLocalTimestamp`（TK-12 评审 M3 的 api 私有实现收敛为共享，测量时刻与核对时刻同校验）；`dto.ts` 增 `SubmitPayloadDto.elevator_checks[]` 与 `ElevatorExpectedDto`（枚举不重立，复用 enums.ts TK-03 的 ElevatorPlanType/ElevatorCheckActual）。**api**：新增 `elevators` 模块（GET /elevators/expected 只读计算，@Roles master,chief）；records.service 提交链路接入——第 1 步并入 elevator_checks 预检（越值/重复/时刻非法/电梯不存在 400 逐台点名）、防呆 409 后「不一致未填说明」409 ELEVATOR_EXPLANATION_REQUIRED、事务内先清后插 elevator_checks（expected 按上送 check_time 重算，D-T22/ELE-05）并逐不一致写 alerts 标红行（elevator_mismatch/mid）；preview 同口径预检（M3/L4 纪律）。**载体决策 D-T22（开工前 AskUser 三问拍板）**：并入提交 payload（离线随队列免费可用）/按 check_time 服务端重算（不信任客户端快照）/任一不一致均标红。**h5**：App.vue 打开电梯卡经 watch 拉取预期（静默降级草稿快照 `elevator_expected` 随持久草稿离线可用）、buildPayload 组装 `elevator_checks[]`、离线预检纳入说明缺失拦截（与 api 同源）；SectionView 电梯卡逐台行（计划说明/预期标签/一致一键确认/不一致选实际状态+说明必填，行锚点 fieldAnchor 同源 #sec-9-elevator-{id}，完成本卡预检点名+跳转定位，C-09 面板抽为两卡型共用）；草稿层新键 `elevator_checks`/`elevator_expected`（D-T18 顺延）；TodayView 电梯卡「已填」以草稿核对项判定（角标分母维持 0/0，D-T16 字段维度不擅改）。**测试**：records-elevator.spec 16 项（ELE-03-T1/ELE-09-T1 单元黄金值 + ELE-02-T1 接口半边/04-T1/T2/05-T1 双时点判别/06-T1 标红/07-T1/回归组）、E2E elevator-check.spec 3 项（预期展示/草稿持久化与锁定不翻转/说明必填点名跳转/payload 捕获）。**文档联动**：决策记录 D-T22+增补 16、契约订正 20（§3.3 两行改写+§4 电梯口径）、技术方案修订 25、台账增补 #30。**评审遗留（本轮自证）**：vant radio 无 aria-checked 属性（E2E 改断言 vant 选中 icon class）；C-09 点名面板原在表单卡分支内，本轮抽共用。门禁：build 三端、lint、format、jest 185/185（重灌种子）、E2E 全量 47/48 + draft-persist 单跑 6/6 复绿（并行竞速 flake 族同修订 #21，未挂账）、check-ledger 全绿。队列下一个任务：M2 清空，M3 TK-18（待确认入口与逐项浏览，依赖 TK-12 已就绪）。

34. **v0.2 修订（2026-09-15）**：TK-17 **独立评审修复轮**（评审发现 2 M + 4 L + 3 m，逐条探针实证，**M1/M2 探针先红后绿**，探针跑完即删并转为正式回归用例）——① **M1 客户端把电梯 409 当「其余 409」吞掉点名清单**：`App.vue` 的 409 分支只认 `need_confirm`，`ELEVATOR_EXPLANATION_REQUIRED`（带 `missing_fields`、无 need_confirm）落进兜底路径——预览弹窗关闭、`missing_fields` 丢弃、只留一句 toast，且 `collectedConfirms` 被清使本轮已收的防呆确认作废（重提再 409 往返）；C-09「逐条点名 + 点击跳转」在电梯场景整体失效。修法：409 带 `missing_fields` 与 400 同处理（抽 `showServerMissingFields` 单一入口，不关窗、不清确认、不重取首页）。② **M2 核对时刻沿用陈旧快照时刻**：`check_time` 取自行上的快照生成时刻，离线或页面挂机后重开会把几小时前的拉取时刻记成核对时刻，服务端按该时刻重算 expected → 预期算错、ELE-06「该停没停」静默漏报、库里留下没发生过的时刻（探针实证：陈旧快照 08:00 被写成 23:30 落笔的核对时刻）；修法按 DATA-13 同口径在**落笔瞬间**取本机时刻，并**用快照自带运行计划本地重算 expected**——UI 语义随之从「与预期一致」改为**记录观察到的实际状态**再与当时预期比对，故时段翻转时自然转不一致（强制说明）并 toast 告知，无法本地重算时（快照缺运行计划）拒记录。③ **L3**：`alerts` 与明细行同快照（重提同事务先清后插，防过期标红累积）。④ **L1**：`elevator_id` 严格整型（禁 `Number()` 宽松转换，'1e2' 会把核对写进另一台真实电梯）；**L2**：脏项点名带序号（原统一兑 `elevator:0`，实证 `[1,2,3]` 产三条同名 → C-09 面板 `:key` 碰撞）；**L4**：接口层补注入固定时刻的黄金值用例（消除「用 shared 函数复算响应值」的同源自证）。⑤ **本轮自查出的文档纪律缺陷**：修订 33 落地时我把新条目插到了前一条**之前**（台账 30→29、契约 20→19、本表 33→32 三处倒序），本轮已交换回升序——「修订记录只追加不删除」含顺序含义，另记 memory。⑥ 未采纳/挂账：预览不提示「尚有 N 台未核对」（m1，判据无此要求，「核对不强制」已在 D-T22/契约 §4 定案，如需提示另走台账新增规格）；字典全停用时前端文案（m3）属同类提示面，暂不改。测试：records-elevator.spec 16→18（L3 重建 + L4 黄金值）、E2E elevator-check.spec 3→5（M1 409 点名可见、M2 落笔时刻与预期重算，用 `page.clock.setFixedTime` 钉时钟保确定性；并改判「刷新后锁定值不翻转」为核对于时刻逐项比对）。**TK-17 状态不变（✅）**；联动：决策记录 D-T22 修订⑤⑥（增补 17）、契约订正 21（含路由计数 34→33）、技术方案修订 26、台账增补 #31。门禁：build 三端、lint、format、jest 全量 **187/187**（重灌种子）、E2E 全量 **50/50**、check-ledger 全绿。队列下一个任务：TK-18（待确认入口与逐项浏览，依赖 TK-12 已就绪）。

35. **v0.2 状态回写（2026-09-15）**：TK-18 完成（⬜→✅）——待确认入口与逐项浏览落地。**shared**：新增 `alerts.ts`（拆条纯函数 `handoverItemsOf`、置顶序权重 `ALERT_LEVEL_RANK`、三类标红行 level 常量，技术方案 §5.3「按条拆分」的单一权威实现）与 dto `PendingListDto`/`RecordDetailDto`/`AlertDto`/`ElevatorCheckRecordDto`；**api**：`GET /records/pending`（我为 receiver 且 status=submitted，逐单 `alert_count` 与 alerts 同源）与 `GET /records/{id}`（alerts **置顶序** level high→mid→low 由服务端排好 + 全部读数 + 电梯核对明细联字典回显 + 双方确认信息，F2-03/F5-01 共用，动态段路由声明在全部静态路由之后）；**提交协议第 ④ 步补全**——契约 §4 原文「生成标红确认行（状态异常/电梯不一致/交接事项拆条）写入 alerts（DEP-08）」此前仅电梯不一致落地（TK-17），本次补齐状态异常（`{field}_bad`/high，异常备注并入文案）与交接事项拆条（`handover_note`/low），与种子 D-1 配套标红行同形；**h5**：首页醒目入口（仅 master 拉取，N=items.length）+ 新增 `ConfirmView.vue`（列表/详情两级：标红置顶高亮按 level 色阶、十板块逐项浏览、电梯核对明细、返回逐级回退）。逐条知晓（acknowledge/confirm）仍随 TK-19。测试：records-pending.spec 7 项（拆条单元 2 + 接口 5，含黄金值哨兵）+ E2E pending-confirm.spec 3 项（UI 与接口响应逐一对照）；jest 全量 **194/194**（重灌种子）、E2E pending-confirm **3/3**。联动：台账增补 #32、契约订正 22。队列下一个任务：TK-19（逐条知晓与签名归档，依赖已就绪）。

36. **v0.2 状态回写（2026-09-15）**：TK-19 完成（⬜→✅）——逐条知晓与签名归档落地。**shared**：dto 新增 `AcknowledgePayloadDto`/`AcknowledgeResultDto`/`ConfirmPayloadDto`/`ConfirmResultDto`（契约 §3.4 形状）。**api**：`POST /records/{id}/acknowledge`（body `alert_ids[]`，逐条写 alerts.acknowledged_by/at；已知晓行不重复写——DB 层 isNull 条件保证首次知晓时刻即留痕不被覆盖；跨单/未知 id 在 where 下不命中即忽略，容错同提交侧 confirmations 消费口径；留痕落 alerts 行本身，不另写审计——契约 §5 审计表无 acknowledge 行，行级归属即审计）与 `POST /records/{id}/confirm`（签名图 PNG data URL 解码校验：仅认 `data:image/png;base64` 前缀 + PNG 魔数、≤512KB → 落盘 `UPLOAD_DIR/signatures/{record_no}.png`、库内记可服务路径 `/uploads/signatures/*.png` 与种子 completed 单同形态；**完整性终校与转 completed 同事务**防「知晓与归档并发」中间态入档；confirmed_at=服务端时刻（DATA-09 同口径）；审计 `record.confirm`；无标红行的单直接可确认——逐条知晓只约束**存在的**确认行）。两路由共用 `confirmableRecordOf` 闸门（单一实现）：404 不存在/非数字 id、403 仅接班人本人（C-05 实名/D-P06 责任锚点，chief 由角色守卫拦）、409 非 submitted（draft/objection/completed 均无确认语义；契约错误码 13 项外不增设新码，归入 CONFIRM_INCOMPLETE 同族、文案区分语义）。**h5**：ConfirmView 标红卡片逐条"已知晓"按钮（已知晓回显服务端时刻不可重复点）、未清零显「尚有 N 项未知晓」置灰签名区；全部知晓后开放 canvas 签名板（pointer 事件兼容鼠标/触屏、清除重签），签名前 C-05 姓名二次确认弹窗；归档成功回列表并刷新计数（completed 不再产生待确认入口，F2-02 取数口径闭环）。**测试**：新增 `records-confirm.spec.ts` 6 项（判据 DEP-08-T1/DATA-08-T1 三类确认行齐全+交接事项 2 行拆 2 条、F2-04-T1 未知晓直接签名 409、F2-04-T2 逐条知晓落库+幂等、F2-05-T1 归档落库+签名图落盘+审计+双方信息可查+待确认入口消失、权限/路由边界、归档后闸门；自建记录不碰种子 D-1，UPLOAD_DIR 注入临时目录）；jest 全量 **200/200**（重灌种子）。**文档联动**：台账增补 #33（F2-04/F2-05/DEP-08）、契约订正 23（§3.4 两行回填）。队列下一个任务：TK-20（异议与版本，依赖 TK-19 已就绪）或 TK-21（撤回窗口，依赖 TK-12）。

37. **v0.2 状态回写（2026-09-15）**：TK-20 完成（⬜→✅）——异议与版本落地。**shared**：dto 新增 `ObjectionPayloadDto`/`ObjectionListDto`/`ObjectionResultDto`/`RecordUpdatePayloadDto`/`RecordUpdateResultDto`/`ResubmitPayloadDto`/`ResubmitResultDto`/`RecordVersionSummaryDto`（契约 §3.2/§3.4 形状）；`RecordDetailDto` 增 `versions` 历史版本摘要（F2-07-T1「历史版本可查」查询面）。**api**：四条路由——`GET /records/mine/objections`（我为 submitter 且 status='objection'）+ `POST /records/{id}/objection`（仅接班人本人 403/D-P06，note 必填 400 点名/超 500 字 400 越界，行锁下转 objection + objection_at + 审计 `record.objection`）+ `PUT /records/{id}`（仅交班人本人，部分合并直写行、status 仍 objection/version 不变；本版本首改时修改前全字段快照定格入 record_versions，UNIQUE(record_id,version) 幂等、changed 按「快照基线 → 当前行叠加本次上送」累计口径——首版误用快照合并丢首改字段，测试实证后修正）+ `POST /records/{id}/resubmit`（仅交班人本人，表单值以 PUT 写入行为准重走 validateForSubmit/防呆/用量计算（与 submit 消费同一套函数），请求体仅可选 confirmations/usage_overrides；上一班缺失时回落原提交 `record.prev_backfill` 审计基线防用量清空；version+1、submitted_at 更新、alerts 先清后插重建（快照语义）、objection_note/at 保留、下游 D+1 重算 trigger=objection_resubmit 检索键 source_record_no）；重提审计复用 record.submit（newValue 记 resubmitted）；状态门控 409 复用 CONFIRM_INCOMPLETE 同族文案区分（错误码 13 项不变）。**TK-16 挂账闭环**：recalcDownstreamInTx 下游行 SELECT ... FOR UPDATE 串行化并发读-改-写 + submitCore 事务 ER_DUP_ENTRY（1062）捕获归一 409 RECORD_EXISTS（契约订正 18 ⑧）；标红重建助手 insertStatusAndHandoverAlertsInTx 自 submitCore 抽取（状态异常/交接事项单一实现）。**测试**：新增 `records-objection.spec.ts` 5 项（F2-06-T1 落库+可见+审计、闸门 403/409/404/401、F2-07-T1 首改快照四要素+累计口径、resubmit 黄金值 12600−12500=100.0 下游重算+version+1+标红重建+versions 摘要、重提防呆 409→confirmations 重提 version 3）；自建记录与 DB 直插下游 D+1 行（submit 只认当前班次），afterAll 全还原。**文档联动**：契约订正 24（异议三行+objection/detail/§5 三行回填）、技术方案修订 27（§4.3/§5.4）、台账增补 #34（F2-06/F2-07 列回填）、决策记录增补 18（**D-T23**）。门禁：build 三端、lint、format、check-ledger 全绿，jest 全量 **205/205**（重灌种子）、E2E 全量 **53/53**（E2E_CHANNEL=chrome）。队列下一个任务：TK-21（撤回窗口，依赖 TK-12 已就绪）或 TK-22（定时任务四件）；TK-21/22 后 M3 清空，M4 自 TK-23（后台框架与权限）起。

38. **v0.2 状态回写（2026-09-16）**：TK-21 完成（⬜→✅）——撤回窗口落地（D-P05 既有拍板，开工前 AskUser 三项定载体：reason 升为响应字段 / 窗口值随 today 回传 / 撤回同事务清 alerts+elevator_checks）。**shared**：`ApiError` 增可选 `reason`（仅 WITHDRAW_NOT_ALLOWED 使用，与已有 `WithdrawNotAllowedReason` 枚举呼应）；dto 新增 `WithdrawResultDto`；`TodayDto` 增 `withdraw_window_minutes`；`calc.ts` 上移 `localTimestampToDate`（localMeasuredAt 逆运算，api 窗口判定与 h5 倒计时同一解析、三端同源）。**api**：`POST /records/today/withdraw`（@Roles master，静态段声明在全部 `:id/xxx` 动态路由之前）——`withdrawableRecordOf` 闸门按当前班次（C-08）+submitter 定位（无行 404、非交班人 403）；`withdraw()` 行锁下校三不可撤条件（**状态锁定先于窗口**：completed→ALREADY_CONFIRMED、objection→IN_OBJECTION、submitted 但超窗口→WINDOW_EXPIRED，均 409 携 reason；draft→409 CONFIRM_INCOMPLETE 同族），成功转 draft+清 submitted_at+同事务清 alerts/elevator_checks（快照语义，与 submitCore 重提先清后插同源）、version 不变、读数列保留、审计 `record.withdraw`；`withdrawWindowMinutes()` 读 configs（限幅 1–1440、回落 10）与 `today()` 回传共用同源；ApiException/ApiErrorFilter 支持 reason 字段。**h5**：client `withdraw()`；App.vue `onWithdraw`（同步在途不接受、成功 toast+loadToday、409 后重取首页反映被确认/异议的新状态）；TodayView 撤回按钮带倒计时（submitted_at + withdraw_window_minutes 算剩余 mm:ss、每秒 tick、到期 canWithdraw 转 false 自隐，onUnmounted 清计时器），与提交入口互斥（submitted vs draft/无）。**测试**：新增 `records-withdraw.spec.ts` 8 项（today 回传窗口黄金值=10、F2-08-T1 转 draft/清 submitted_at/审计留痕 + F2-09-T2 待确认入口消失 + draft 重复撤回 409、F2-08-T2 重提 version+1、F2-10-T1 窗口过期 reason=WINDOW_EXPIRED、闸门 403/401、F2-10-T3 reason=IN_OBJECTION、F2-10-T2 reason=ALREADY_CONFIRMED、404）；窗口过期/已确认两态以 DB 直改 submitted_at/status 构造（confirm 全流程已由 records-confirm.spec F2-05-T1 覆盖），自建记录不碰种子 D-1、afterAll 全还原；E2E `withdraw.spec.ts` 2 项（F2-09-T1 倒计时≈满窗口 + 点击撤回→draft→提交入口重现、窗口已过→撤回入口不显）。**文档联动**：契约订正 25（§2 reason 字段+错误码表注、§3.2 today/withdraw 两行回填、§5 record.withdraw 行）、台账增补 #35（F2-08/09/10 列回填、F2-10 ⬜→✅）。门禁：build 三端、lint、format、check-ledger 全绿，jest 全量（重灌种子）、E2E（E2E_CHANNEL=chrome）。队列下一个任务：TK-22（定时任务四件，依赖 TK-12 已就绪）；TK-22 后 M3 清空，M4 自 TK-23（后台框架与权限）起。
39. **v0.2 状态回写（2026-09-16）**：TK-22 完成（⬜→✅）——服务端定时任务四件落地，M3 随此清空。**shared**：dto 新增 `NOTIFICATION_KINDS`/`NotificationDto`/`NotificationListDto`/`NotificationReadResultDto`（契约 §3.5 形状）。**api**：新增 `notifications` 模块——① 读侧契约 §3.5 两行路由：`GET /notifications`（当前登录人未读通知 id 倒序 + unread 总数，DEP-04 未读角标数据源）与 `POST /notifications/{id}/read`（幂等回首次时刻；非本人/不存在 404 不泄露存在性、非数字 id 同 404 结构；仅 SessionGuard，契约角色列「登录用户」）；② 写侧四扫描（**时间可注入**：now 由调用方传入，「定时」层判据直调扫描本体）——`runConfirmDueScan`（F2-11：status='submitted' 且 submitted_at+阈值已过 → 提醒接班人；去重键 (kind,record,receiver) 只认晚于当前 submitted_at 创建的通知——撤回重提刷新窗口后旧提醒不压制；**notifications.createdAt 系库端 CURRENT_TIMESTAMP（连接 timezone='Z' → UTC 串）**，与 records.submittedAt 的本地墙钟串（localMeasuredAt 口径）比较经 shared localTimestampToDate 换算，防 8 小时口径差误判）、`runObjectionEscalationScan`（F2-12：status='objection' 且 objection_at+阈值已过 → escalated_at **原子闸门**（UPDATE … WHERE escalated_at IS NULL，命中行数 1 才发通知，并发扫描亦只成功一次）+ 提醒全部 active 科长）、`runMissingSubmitScan`（F6-06：排班日 D 过（D+1）日 deadline 仍无 records 行 → 提醒科长；**漏交判定以 records 行存在为准**（增补 #27，draft 撤回未重提不算漏交）；**扫描窗口 = 当前班次 − configs backfill_window_days**（窗口外无法经补交端点补救，提醒无对象且防历史排班刷屏，D-T21 L3 对齐）；**去重键 (chief, title)**（title=`{duty_date} 应提交未提交` 兼作幂等键）；**截止判定在墙钟空间**（(D+1) 日 'HH:MM' 与 now 的当地墙钟直接比较日历日与分钟——duty-date.ts 新导出 `localWallClock`，不引入第二个时区换算实现））、`runSessionsCleanup`（DELETE sessions WHERE expires_at ≤ now；sessions 列为 UTC 串口径，与 records 列本地串**两套口径各随其列**，比较同空间）；③ 生产调度 `notifications.scheduler.ts`：@nestjs/schedule（技术方案 §2 选型「官方定时任务」）按 5/30/30/60 分钟间隔喂系统时钟，jest 环境 inTest 闸静默（NODE_ENV/JEST_WORKER_ID 双闸），扫描失败记日志等下轮（幂等自动补齐）；④ 配置读取回落与限幅：confirm_due_hours 限 1–72（默认 2）、objection_escalate_hours 限 1–168（默认 24）、missing_submit_deadline 'HH:MM'（默认 09:00）——三键种子已有，无新增键；⑤ AppModule 接入 ScheduleModule.forRoot() + NotificationsModule；RecordsModule 导出 RecordsService 供复用 resolveDutyDate/backfillWindowDays（C-08 班次分界与补交窗口单一实现，防另写日历推算）。**测试**：新增 `notifications.spec.ts` 7 项（F2-11-T1 阈值未到不提醒/过阈值提醒/重复扫描不重复 + GET/POST read 幂等与未读回落 + 闸门 404/401；F2-12-T1 24h 内不升级/过 24h escalated_at 落库+科长通知/重复扫描不重复；F6-06-T1 **时点可配两轮扫描**（deadline 临时调 23:59 → D+7 未到点；还原 09:00 同刻扫描 D+7 恰到点，通知含日期+排班人，期望值从库动态计算不硬编码）+ 窗口外不提醒；会话清理过期删/未过期留）。测试注入 now = 当前班次 + 8/16 天上海正午（UTC+8 恒定换算），**不触碰任何种子行**；beforeAll 以真实时钟跑一轮 runAll settle 种子 D-2 长挂异议演示单（首轮升级本就是 F2-12 语义），落定通知在水位线上由 afterAll 清除、escalated_at 还原；missing_submit_deadline 临时改值 afterAll 还原（records-backfill.spec 同法）。**文档联动**：契约订正 26（§3.5 两行回填）、技术方案修订 28（§5.4/§6 落点）、台账增补 #36（F2-11/F2-12/F6-06 列回填）。门禁：build 三端、lint、format、check-ledger 全绿，jest 全量 **220/220**（重灌种子）、E2E 全量 **55/55**（E2E_CHANNEL=chrome；本机缺 Playwright headless shell 1234 内核——纯环境项，与实现无关）。队列下一个任务：M3 清空，M4 自 TK-23（后台框架与权限，依赖 TK-04 已就绪）起。

40. **v0.2 状态回写（2026-09-16）**：TK-23 完成（⬜→✅）——后台框架与权限落地，M4 起步。**shared**：dto 新增 `MissingSubmitItemDto`/`MissingSubmitListDto`（契约 §3.6 形状：duty_date/user_id/real_name，items 按 duty_date 倒序）。**api**：新增 `admin` 模块——① 类级守卫框架：`@Controller('admin')` 上统一 `@UseGuards(SessionGuard, RolesGuard)` + `@Roles('chief')`，未登录 401 先于角色判定、master 访问一律 403 FORBIDDEN（C-05 接口侧执法；TK-24~29 后台控制器沿用同一类级三行）；② 首条路由 GET /admin/missing-submits（F6-06 后台视图半边，PRD §6.6「科长打开后台 → 显示应提交未提交提醒（日期+排班人）」）——漏交判定自 notifications.service 抽取为 `missingSubmitShifts(now)` 单一实现（扫描与视图共用：截止在墙钟空间、records 行存在即不算漏交 #27、窗口=当前班次−backfill_window_days D-T21 L3；now 可注入，runMissingSubmitScan 改为消费该判定，行为不变），AdminService 倒序组装；③ admin.spec **8 项**——路由完备性哨兵（经 container.getModules() 枚举实际注册的 /admin 路由，与契约 §3.6 当前落地清单逐字对账；TK-24~29 新挂后台路由未扩入权限矩阵即红）+ 权限矩阵三向（master→全部 403 FORBIDDEN、未登录→全部 401 UNAUTHENTICATED、chief→非鉴权类失败；断言串入路由 URL 便于定位）+ 视图黄金值（注入 now=当前班次+8 天上海正午 → 缺失集合=D+1~D+7 倒序、排班人姓名与 users 表同源；种子哨兵防跨灌种日漂移）+ #27 视图口径（直插 D+3 draft 行 → 视图剔除，try/finally 还原）+ 窗口外不展示（种子 D-14~D-11 排班无记录恒不入视图）+ 路由层冒烟（GET body 与 service 同刻结果逐字一致）。**admin**：apps/admin 骨架——api client（fetch + HttpOnly Cookie 通道 + ApiRequestError/NetworkError 严格区分，同 h5 口径精简版）、登录页（C-05 实名制，登录事件服务端记审计随 TK-04）、/auth/me 会话恢复、role≠chief 前端拦截至无权限页（服务端 403 为最终执法者）、401 会话失效单一入口 handleSessionLoss（与 h5 resetSession 同纪律）、Element Plus PC 布局（侧边四页导航 + 顶栏用户区 + 主内容区，对齐 demo 科长后台 v0.1；业务内容随 TK-24~29 逐页填充，占位页标注落点任务号；不引 vue-router——与 h5 同款组件状态切换，不为骨架页新增依赖）。**E2E**：playwright 新增 desktop-admin 项目（Desktop Chrome :5174，testMatch admin-*；mobile-h5 项目 testIgnore admin-*），admin-shell.spec 3 项（chief 登录→四页导航+用户区+应提交未提交卡+登出；master 登录→无权限页；未登录直开→回登录页；真链路不 mock 登录/登出）。**文档联动**：契约订正 27（§3.6 missing-submits 行回填+守卫框架口径留痕）、技术方案修订 29（§3 模块划分补 admin 落点）、台账增补 #37（F6-06 技术方案列回补后台视图半边；C-05 无独立 -T 用例、以权限矩阵为 TK-23 判据自动化载体）。门禁：build 三端、lint、format、jest 全量 **228/228**（重灌种子）、E2E 全量 **58/58**（E2E_CHANNEL=chrome）、check-ledger 全绿。队列下一个任务：TK-24（记录管理）或 TK-25（人员管理），均依赖 TK-23 已就绪。

41. **v0.2 状态回写（2026-09-17）**：TK-24 完成（⬜→✅）——记录管理（F6-01 查看/按日期与交班人筛选/导出/批注）落地，决策记录 **D-T24**（增补 19）五项口径（批注覆盖式单条 chief_note + 空串清除、不限记录状态、审计 record.annotate 留痕不进 record_versions、导出 Phase 1 CSV 带 BOM、GET /records 列表随 F6-01 半边先行）。**shared**：dto 新增 `RecordListItemDto`/`RecordListDto`/`AnnotationPayloadDto`/`AnnotationResultDto`，`RecordDetailDto` 补 `chief_note`；duty-date.ts 新增 `isValidCalendarDate`（日历校验单一权威，records.service 的 calendarDateOf 改为消费它，行为不变）。**api**：① `GET /records`（契约 §3.5 既有行）：RecordsService.list/exportRows 共用 listWhere 与 parseRecordListFilters（四参数筛选，非法 400 逐条点名；status 越值 400、日历日校验拦 02-30 等；不分页，duty_date 倒序，alert_count 一次 group by 不 N+1，与 pending 同口径），角色 master+chief（登录用户，同 detail 口径）；静态段 `@Get()` 声明在全部 `:id` 动态路由之前（防吞并）；② `GET /admin/records/export`：AdminController 薄委托 AdminService.recordsExportCsv（@Res 直写 CSV 附件，UTF-8 BOM + CRLF，14 列 EXPORT_COLUMNS 钉死，文件名按筛选区间 records-YYYY-MM.csv / records-{from}_{to}.csv / records-{当日}.csv；xlsx/正式月报挂 P2 F5-04），取数复用 RecordsService.exportRows（多带四项日用量原值）；③ `POST /admin/records/{id}/annotation`（**契约订正 28 新增路由，总数 34→35**）：RecordsService.annotate 单一实现（行锁覆盖式单条 chief_note、trim 空串=清除、500 字越界 400 点名 chief_note、值无变化不更新不写审计、审计 `record.annotate` oldValue/newValue 携 note 前后值、404 口径与 detail 同族），AdminModule 引入 RecordsModule 复用（记录路由与后台批注永不两套口径）；④ schema records 补列 `chief_note` VARCHAR(500)（迁移 0002_loose_black_bird.sql，仅一列无回填）。**admin**：apps/admin 记录管理页——api client 补 recordsList/recordsDetail/annotate/exportRecords（CSV 走独立 fetch 通道：失败解析契约 §2 错误体、成功 Blob 触发浏览器下载，文件名取 Content-Disposition）；页面 = 应提交未提交卡（TK-23 既有）+ 交接记录卡（默认近 30 天 daterange + 交班人候选自当前行聚合（人员全表随 TK-25 后可换源）+ 状态筛选 + 列表（状态 tag/标红数/批注列）+ 行点击详情抽屉（descriptions 基本信息批注区标红项十板块读数按 shared 字段字典分组空值不展示）+ 批注对话框（maxlength 500、空文本=清除、保存后列表同步刷新）+ 导出按钮）。**E2E**：admin-records.spec 3 项（列表 10 行 + status 筛选 1 行 + 详情抽屉含 HB- 单号与标红区；批注真链路写入→详情/列表回读→空文本清除回「暂无批注」；导出等待 download 事件、文件名 records-*.csv）。**测试**：admin-records.spec **15 项**（F6-01-T1：无筛选黄金值与行形状键集、from/to 窗口与库内查询逐字一致、submitter_id 从库反查期望、status 与组合筛选、非法参数四例在列表与导出两端 400 点名、CSV 月内窗口 BOM/CRLF/表头逐字/行数对应、跨月与无筛选文件名、空结果仅表头、批注写入三处回读+审计 null→值、覆盖值→值+同值不写审计、清除值→null+trim、501 字越界、未知/非数字 id 404、draft 行可批、种子还原）+ admin.spec 哨兵扩至 **3 条路由**（权限矩阵自动覆盖新路由）；jest 全量 **243/243**（重灌种子）、E2E desktop-admin **6/6**（E2E_CHANNEL=chrome）。**文档联动**：契约订正 28（§3.5 GET /records 要点回填、§3.6 导出行交付形态钉死+新路由 annotation、§3.4 详情行补 chief_note、§5 审计表新增 record.annotate、路由 34→35）、技术方案修订 30（§4.2 records 补列 chief_note、§3 模块划分补 TK-24 落点）、台账增补 #38（F6-01 技术方案列回填、F5-01 补注接口半边已落地）、决策记录增补 19（D-T24 + 拍板状态说明 + 挂账移交）。**挂账移交（本轮未做，D-T24 尾注留痕）**：科长代录他人（D-T21 ② 拍板仍有效、需求未确认）与 `/records/backfill/preview`（TK-16 遗留、无消费者）移挂 h5 滞留单处置面板（自助补交，TK-15 L2 处置面板入口）落地时一并评估。门禁：build 三端、lint、format、jest 全量 243/243（重灌种子）、E2E 全量、check-ledger 全绿。队列下一个任务：TK-25（人员管理）或 TK-26（排班管理与安全阀）。
