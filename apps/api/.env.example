# 数据库连接（TK-02）。复制为 .env 并填实际凭据；.env 已在 .gitignore，不入库
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=handover

# 生产由 Nginx 同源反代时设 true（按 1 跳采信 X-Forwarded-For，审计记真实客户端 IP，C-05）；
# 直连/开发环境保持 false，防止伪造请求头污染审计 IP
TRUST_PROXY=false
