# PRD — Handover · 新院区总务交接班数字化系统（讨论稿 v0.2.5）

- **项目代号**：Handover（已确认）
- **正式名**：新院区交接班管理系统
- **中文日常称呼**：建议叫"交班"（师傅口语："今天交班填了没"）
- **生成时间**：2026-08-27（2026-08-31 更新至 v0.2.5）
- **输入来源**：纸质《新院区交接班》填写案例 1 份 + 需求确认 4 项 + 待澄清问答 8 项
- **完整度**：moderate（移动端设计稿与埋点清单待生成）
- **状态**：主要澄清项已闭环；v0.2.5 液氧两罐轮换模型落稿（换罐线+在用罐动态）；余待核实/待确认事项见文末剩余待办

---

## v0.2.5 修订记录（2026-08-31，总务科补充业务模型）

1. **液氧两罐轮换模型落稿**：实际运行为"两罐皆满、启用一罐、用到切换线换另一罐"。新增中预警：**在用罐含量 < 2.5（换罐线，后台可配）→ 提示切换罐**，与既有 < 2.0 补液线构成双档阈值（见 6.4）
2. **在用罐表达动态化**："使用罐号"为枚举选择（1 号/2 号），各液氧卡片标题随选择动态显示"在用/备用"，不再写死；日间用量按所选在用罐取数（见 6.4、附录 A）
3. **1 号罐压力补录**：两罐两时点的含量/压力均为必填（附录 A 原有口径，demo 曾漏 1 号罐压力，已补齐）

---

## v0.2.4 修订记录（2026-08-31）

1. **排班范围定案（最小嵌入 + 安全阀）**：登录为事实身份来源、排班为期望身份来源，二者对账。保留最小"日期→人"排班表（科长维护、改即审计），驱动接班人自动带出与漏交检测（排班存在但无提交 → 科长后台提醒）；新增安全阀：登录提交人与当日排班不符时提示确认并留痕，覆盖未登记调班场景。明确不建：换班申请/审批流、请假管理、排班统计、轮转模板自动生成（见 §2、§6.6）

---

## v0.2.3 修订记录（2026-08-29）

1. **首页卡片制信息架构落稿**：Demo v0.2 的评审结论正式写入文档——首页为按巡检点位/时段组织的 12 张任务卡（液氧拆为 8:30 / 20:30 两张到点卡），点卡片进入板块填写页，角标实时显示已填/待填/异常；替代 v0.2 的"从上到下单向填写"表述（见 §6.0、§6.1；与技术方案 §10 对齐）
2. **撤回窗口补全进 F2 全文**：功能描述补入撤回入口（此前仅在验收标准中），设计触点新增"撤回按钮（带倒计时）"（见 6.2）
3. **天然气充气口径与技术方案对齐**：由"当日用量按 0 计"更正为"**该卡**当日用量按 0 计、另一卡正常计算"（见 6.1 边缘情况；以技术方案 §4.3 单卡判定为准）

---

## v0.2.2 修订记录（2026-08-28，总务科拍板）

1. **新增提交后撤回窗口**：交班人提交后 **10 分钟内、且接班人尚未确认**时，可单方撤回交接单回到可编辑状态，修改后重新提交（版本+1，撤回动作全程留痕）；接班人一旦确认即锁定，此后只能走异议退回流程。动机：填错难免，短窗口纠错比一律走异议更近人情。边界：窗口过期、接班人已确认、交接单处于"有异议"状态，三者任一满足即不可撤回（见 6.2）

---

## v0.2.1 修订记录（内部评审修订，2026-08-27）

1. **离线提交语义修正**：原验收标准"离线期间提交 → 接班人侧标注'数据同步中'"存在逻辑矛盾——数据未上传前服务器无感知，接班人端不可能显示任何状态。改为**"上传成功即正式提交"**：离线提交进入本地待同步队列，同步成功前对接班人不可见，并增加未同步强提醒（见 6.1）
2. **Phase 1 范围补齐**：电梯字典与运行时间表维护并入 F6 精简版（原属 Phase 2 完整配置中心，但 Phase 1 的电梯核对依赖它）；明确 Phase 1 电梯不一致的处理形态（交接单标红 + 必填说明，预警中心随 Phase 2 上线）；"异常自动标记率"达成时间改为 Phase 2 上线（见 §4、§8、6.7）
3. **液氧用量口径显式化**：原"每日液氧用量"实为同记录内 8:30→20:30 的 12 小时差值，夜间消耗（20:30→次日 8:30）不计入任何一天。更名为"液氧日间用量"并注明沿用纸质口径；新增"液氧夜间用量"（跨记录差值）建议项待确认；防泄漏预警扩展至两个 12 小时窗口（见 6.3、6.4、附录 A）
4. **液氧计量单位存疑标注**：原案例单位 L 与医院液氧储罐量级明显不符（疑似 m³、吨或液位计米数），"2.0 L 已确认"降级为"数值已确认、单位待核实"并列入待办优先项；单位若错，阈值/用量/预警数值全部联动修正（见 6.4、附录 A、剩余待办）
5. **时间语义定义**：记录"日期"=班次起始日（非提交日），趋势与检索一律以班次日期为准；电梯预期状态改为按**核对时刻**计算并在提交时锁定不重算，消除"核对时一致、提交时预期翻转"的假预警；补充核对时机建议（见附录 A 基础信息、6.7）
6. **流程与责任链补强**：接班人字段修改需填原因留痕；异议退回增加历史版本留痕与 24 小时超时升级；风险表新增"账号共用""站内提醒触达"两条（见 6.2、附录 A、§7）
7. **待办补全**：新增轮值师傅人数、部署方案确认两项未闭环事项，头部状态表述同步修正
8. **笔误修正**："1/2 号液氧罐"歧义消除；"按时段停运"更名"按时段运行"；液氧 8:30 读数补充上一班值比对说明；概要中液氧阈值不再绑定存疑单位
9. **文字修正与边缘补全（同日补充修订）**：新增天然气充气（剩余量增大）边缘情况与防呆说明；明确 Phase 1 标红项同样走接班人逐条"已知晓"确认；F2 异议处理改为兼容轮值制的"下次到岗"表述；交接时间口径明确（以同步成功为准、重新提交即更新）；§2 液氧案例数值改为"单位待核实"；薄弱章节提示去除内部工具命名

---

## v0.2 变更记录（相对 v0.1）

1. **班制确认**：24 小时班，一天一条交接记录；液氧 8:30 / 20:30 两次读数属于同一条记录内的两个时点
2. **用电量口径确认**：每日用电量 = 如意线差值 + 工贸线差值（两线之和）
3. **液氧预警阈值确认**：2.0 L
4. **电梯重设计**：由"每班手写停运清单"改为"电梯字典 + 运行时间表 + 预期状态核对"（见 6.7）
5. **新增离线填写**（P0，地下表房信号弱）与**可选拍照留证**（F7，P1）
6. **登录与对接**：账号密码登录，暂不对接企业微信/钉钉，预留接口

---

## 1. 概要（60 秒版本）

Handover 把总务科新院区纸质《交接班》表单升级为**移动端优先的 Web 系统**：值班师傅（24 小时班）用手机在巡检途中完成水、电、天然气、医用气体、供暖冷、水泵、新风空调、电梯等 10 大类运行数据的填写，系统自动计算每日用量、自动高亮异常项（液氧含量低于阈值、气瓶低于基数、压力越限、设备异常），次日接班师傅在线逐项确认并签名，形成可追溯的电子交接档案，科长在管理后台查看全部记录、趋势与报表。目标是消灭手工计算与人工盯防的遗漏，让院区运行数据可查、可溯、可分析，单次交接耗时从约 20 分钟降至 10 分钟以内。

---

## 2. 背景与问题

**现状**：新院区交接班依赖纸质表单，每天值班的师傅手写 10 大类巡检数据，接班人阅后签字，纸质归档。

**从师傅视角看，问题有四个**：

其一，每日用量靠手算。水表、双路电表、天然气、液氧的"每日用量"都等于两次读数之差，纸面时代靠师傅现场心算或按计算器，慢且易错。

其二，异常靠人眼盯。案例中已出现明确信号——二氧化碳满瓶 5 瓶、基数 20 瓶，属于明显的待换气信号；2 号液氧罐含量一班内从 2.3 降到 1.8（原记录单位 L，待现场核实，见剩余待办 1）——但纸质表单不会标红，全靠接班人经验发现，漏看即成隐患。医用氧气是生命支持相关资源，这类遗漏的代价不对称。

其三，数据沉没在纸堆里。无法做趋势分析（例如液氧消耗速率直接决定补液氧采购时点），事后追溯要翻档案，月度用量统计靠人工誊抄。

其四，交接责任靠口头与签名。没有逐项确认机制，出了问题难以界定班次责任。

**为什么是现在**：医院信息化建设在推进，师傅人手一部手机，H5 技术门槛与部署成本都很低；新院区投用后设备点位多、数据量大，纸质模式的维护成本正在快速上升。

**本期明确不做**：表计传感器/物联网自动采集（保留数据接口可能）、维修工单流程、库存采购流程、老院区接入（已确认暂不接入）、企业微信/钉钉对接（暂不，预留接口）、完整排班模块（换班申请/审批、请假、统计、轮转模板——仅保留最小排班表，见 v0.2.4 修订记录）。

---

## 3. 用户与角色

### 主要用户：值班师傅（数名轮值，如张师傅、施师傅）

```
身份：总务科新院区值班师傅，24 小时班轮值，每天一人在岗（轮值人数待确认，见剩余待办）
情境：当班期间按固定路线巡检表房、液氧站、泵房、锅炉房、制冷机房等点位，
     其中液氧罐需在 8:30 与 20:30 各测一次；班末填写交接单，次日交接
JTBD：当巡检和交接时，师傅想快速准确地记录院区运行数据并交给下一班，
     从而责任清晰、异常不漏、不用回头补录。
当前 workaround：纸上手写 → 手算用量 → 口头+签字交接 → 纸质存档
痛点：字段多写起来慢；用量手算易错；异常靠记性；历史查不到
```

### 次要用户：总务科科长（管理员）

```
身份：总务科负责人
情境：管理多名轮值师傅，对院区运行安全与能耗成本负责
JTBD：当管理院区运行时，科长想看到全部交接记录、异常统计与能耗趋势，
     从而决定补液氧/换气瓶等采购时点、排班与考核。
痛点：纸质记录无法汇总统计，异常发现滞后，考核无据
```

### 不为谁做

暂不为临床科室、第三方维保商、老院区人员设计（老院区已确认暂不接入）。

---

## 4. 目标与成功度量

**产品目标**：让一次交接班在手机上完整完成，做到"填写即校验、异常即高亮、记录即可溯"。

| 指标 | 度量什么 | 目标值 | 时间窗 |
| --- | --- | --- | --- |
| 电子交接率 | 使用系统完成交接的班次占比 | 100%（纸质停用） | 上线后 30 天 |
| 单次填写耗时 | 从开始填写到提交 | ≤ 10 分钟 | 30 天 |
| 异常自动标记率 | 达到阈值的项被系统标记的比例 | 100% | Phase 2 上线即达成（Phase 1 仅表单级标红） |
| 交接确认完成率 | 接班人按时完成确认的记录占比 | ≥ 95% | 60 天 |
| 数据可追溯率 | 可查到历史班次完整记录的比例 | 100% | 上线即达成 |

**反指标**：不优化"预警数量"。靠调低阈值制造满屏红点只会摧毁师傅对预警的信任——考核预警的"准确率"（真实异常占比），而不是数量。

**关键假设**：师傅愿意用手机填写，且手机输入不比手写慢。如果这条假设错了，系统就会被弃用——因此 MVP 的第一原则是填写效率，一切设计为它让路。

---

## 5. 核心价值

**对师傅**：终于可以边巡检边完成交接、让系统替自己算用量、标异常，而不必手算读数差、担心漏看异常项，因为系统带着上一班数据自动回填和比对。

**对科长**：终于可以在一个后台看到院区运行的完整脉搏，而不必翻纸质档案、手抄月度用量，因为所有交接记录自动沉淀为可筛选、可导出、可画趋势的数据。

**与通用表单工具的差异**：通用表单没有"班次连续性"概念——Handover 的用量计算依赖上一班读数、预警依赖可配置基数与阈值、交接依赖双向确认流程、电梯核对照运行时间表自动生成预期状态。这是运行巡检模型，不是表单收集。

---

## 6. 功能范围（核心章节）

### 6.0 总体信息架构

| 页面 | 端 | 说明 |
| --- | --- | --- |
| 今日交接（首页任务卡） | 手机 | 核心入口：按巡检点位/时段组织的 12 张任务卡（液氧拆 8:30 / 20:30 两卡），点卡片进入板块填写页，角标实时显示已填/待填/异常 |
| 交接确认页 | 手机 | 次日接班人逐项确认 + 电子签名 |
| 预警中心 | 手机 + 后台 | 当前预警与历史预警 |
| 历史记录 | 手机 + 后台 | 按日期/交班人筛选查看 |
| 趋势报表 | 后台 | 用量曲线、液氧消耗、气瓶库存、月报导出 |
| 管理后台 | PC Web | 人员、排班、阈值、基数、电梯字典与时间表、点位字典 |

### 6.1 F1（P0）移动端交接班表单填写

**Persona**：值班师傅
**优先级**：P0
**关键假设标记**：⭐（填写效率决定生死）

**功能描述**：师傅登录后首页即"今日交接"任务卡页（一天一条记录）。首页按巡检点位组织 12 张任务卡（表房、液氧站、锅炉房、泵房、电梯等），液氧拆为 8:30 与 20:30 两张"到点卡"；点卡片进入该板块填写页，只看到本板块的几项字段，填完点"完成"返回首页；卡片角标与顶部进度条实时汇总已填/待填/异常。数值字段自动弹出数字键盘；上一班的读数字段自动回填显示供比对；液氧罐测完一次填一次；页面顶部显示填写进度。支持草稿自动保存与离线填写。

**In Scope**：

- 十个板块的完整结构化填写（字段明细见附录 A 数据字典）
- 上一班读数自动带出、每日用量自动计算（水/电/气/液氧）
- **离线填写**：地下表房等弱信号点位可先填写暂存本地，恢复院内网络后自动上传，同步状态全程可见；**离线状态下点击提交仅进入本地"待同步"队列，上传成功那一刻才算正式提交**，同步成功前该记录对接班人不可见
- 必填校验、数值范围校验、单位标注
- 草稿自动保存与续填
- 提交前汇总预览：未填项、异常项一目了然
- 登录：账号密码（系统发号），暂不对接企业微信/钉钉，预留接口

**Out of Scope**：

- 语音填写
- 扫码自动读数（物联网方向，远期）

**验收标准**：

- Given 上一班水表读数为 48939 吨，When 师傅输入本次读数 49239 吨，Then 系统显示"每日用水量 300 吨（自动计算）"
- Given 师傅处于无网络的地下表房，When 填写并暂存，Then 数据保存在本机，回到有网络处自动同步并显示"已同步"
- Given 师傅未填写"高配房是否正常"，When 点击提交，Then 阻止提交并定位到该必填项
- Given 系统无任何历史数据（首次启用），When 填写读数，Then 每日用量显示"—"并提示"首班记录，无上一班数据可比对"
- 边缘情况：输入明显异常值（如水表读数比上一班小）→ 弹出确认"本次读数小于上一班，是否确认？"
- 边缘情况：天然气剩余量大于上一班（可能发生充气/购卡）→ 弹出确认；确认充气后**该卡**当日用量按 0 计并留痕充气备注，另一卡正常计算，避免出现负用量
- 边缘情况：离线状态下点击提交 → 交接单进入本地"待同步"队列并提示"尚未完成交接，请回到院内网络完成同步"；同步成功前该记录对接班人不可见（服务器无感知）
- Given 本地待同步的交接单上传成功，When 同步完成，Then 交班人侧显示"已提交"，接班人首页出现待确认入口
- 边缘情况：交接单长时间停留在"待同步"（如离院前未回到有网络区域）→ 数据仅存于本机，次日到岗后首要提醒完成同步；培训明确"看到'已同步'方可下班"

**设计触点**：移动端 10 板块表单、数字键盘、自动回填卡片、离线状态条、未同步强提醒、提交预览页、草稿恢复提示

### 6.2 F2（P0）交接班确认流程

**功能描述**：24 小时班制下，每天一条交接记录。交班人（当日值班师傅）提交后生成正式交接单，提交后 10 分钟内且接班人尚未确认时可撤回修改（见验收标准）；接班人（次日值班师傅，系统按排班表自动带出）打开待确认交接单，逐项浏览（预警项置顶高亮；Phase 1 尚无独立预警，"预警项"指表单级标红项，见 §8），确认无误后签名提交，记录归档为"已完成"；发现问题可标注异议并退回。

**验收标准**：

- Given 交班人已提交，When 次日接班人登录，Then 首页显示醒目入口"有 1 份交接单待确认"，接班人姓名已按排班表自动带出
- Given 交接单含预警项，When 接班人确认，Then 预警项要求逐条点击"已知晓"后方可签名
- Given 接班人确认完成，When 任何人查看该日记录，Then 显示双方姓名、确认时间与签名
- Given 接班人发现数据有误，When 标注异议并填写原因，Then 交接单状态变为"有异议"，退回交班人修改后重新提交
- 边缘情况：接班人超过设定时限（建议 2 小时）未确认 → 站内提醒（暂不接第三方消息渠道）
- Given 交接单因异议被退回，When 交班人修改后重新提交，Then 保留全部历史版本（修改人、修改时间、修改字段）供追溯
- Given 交班人刚提交且接班人尚未确认，When 10 分钟内点击"撤回修改"，Then 交接单回到可编辑状态，接班人端待确认入口同步消失，撤回动作留痕（谁、何时）；重新提交后版本号+1
- 边缘情况：超过 10 分钟窗口、或接班人已确认、或交接单处于"有异议"状态 → 撤回入口不可用，提示"请联系接班人通过异议流程处理"
- 边缘情况：退回异议时交班人已离院（内网部署下无法在家修改）→ 交接单停留在"有异议"状态、不影响当班运行，交班人下次到岗时处理（轮值制下未必是次日，由 24 小时升级机制兜底）；超过 24 小时未解决自动升级提醒科长

**设计触点**：待办入口、逐项确认列表、预警置顶区、电子签名板、异议标注表单、撤回按钮（带 10 分钟倒计时）

### 6.3 F3（P0）用量自动计算

**功能描述**：系统依据"本次读数 − 上一次同字段读数"自动计算每日用量，覆盖水、电、天然气、液氧四类。计算结果标注"自动计算"标识，允许手工覆盖但必须填写原因（留痕）。

**计算规则（v0.2 已确认）**：

| 项目 | 规则 | 状态 |
| --- | --- | --- |
| 每日用水量 | 本次水表读数 − 上一班水表读数 | 确认 |
| 每日用电量 | **如意线差值 + 工贸线差值（两线之和）**，分线差值同屏展示 | **已确认** |
| 每日天然气用量 | 主卡+副卡剩余量的减少值（剩余量递减，方向与水表相反，需防呆；剩余量增大按充气处理，见 F1 边缘情况） | 确认 |
| 液氧日间用量 | 在用罐两次读数（8:30 与 20:30）含量差为自动推荐值，换罐/补液时可手工修正；**仅覆盖 8:30–20:30，沿用纸质表口径** | 确认（口径已显式化） |
| 液氧夜间用量（建议新增） | 昨日 20:30 → 今日 8:30 含量差（跨记录自动计算）；与日间用量相加为全天用量，单独用于夜间防泄漏监测 | 待总务科确认 |

**验收标准**：

- Given 上一班如意线 21000、工贸线 16300，When 本班输入 21400 与 16670，Then 显示"如意线 +400，工贸线 +370，每日用电量合计 770（自动计算）"（数字仅为口径示例）
- Given 师傅手工覆盖自动计算值，When 提交，Then 系统强制填写覆盖原因并留痕
- 边缘情况：上一班数据缺失 → 显示"—"并允许补录上一班读数

### 6.4 F4（P1）阈值预警

**功能描述**：提交时系统按阈值规则逐项比对，命中即生成预警：交接单上标红、进入预警中心、推送科长（站内）。所有阈值后台可配置，支持按月复盘调整。

**预警规则表**：

| 监控项 | 规则 | 级别 | 状态 |
| --- | --- | --- | --- |
| 液氧罐含量 | < **2.0（数值已确认；单位待现场核实，原记录为 L，疑似 m³/吨/液位米）** | 高——提示安排补液氧 | 数值已确认，单位待核实 |
| 液氧换罐 | 在用罐含量 < **2.5（后台可配）** | 中——提示切换罐 | 确认（v0.2.5 新增，两罐轮换模型） |
| 液氧罐压力 | 超出区间（建议 0.8–1.3 MPa，后台可调） | 高 | 初始值待科长定稿 |
| 气瓶满瓶数 | < 基数 | 高——提示安排换气 | 规则确认，基数后台配置 |
| 汇流排/各出口压力 | 超出各点位设定区间（逐点配置） | 中 | 初始值待科长定稿 |
| 高配房 / 锅炉房 / 负压 / 空气 | 状态=异常 | 高 | 确认 |
| 生活热水出口水温 | 偏离设定值 ±3°C | 低 | 确认 |
| 液氧时段降幅 | 任一 12 小时窗口（8:30→20:30 或 20:30→次日 8:30）降幅超 0.8（防泄漏，后台可调；夜间窗口依赖"液氧夜间用量"口径确认） | 中 | 建议值，夜间窗口待确认 |
| 电梯未按时间表运行 | 预期停运时段内实际仍在运行（见 6.7） | 中 | 依赖电梯配置 |

**验收标准**：

- Given 二氧化碳满瓶输入 5、系统基数 20，When 提交，Then 生成预警"二氧化碳满瓶低于基数（5/20）"，交接单该项标红，科长收到站内推送
- Given 液氧含量输入 1.8，When 提交，Then 触发预警"1 号或 2 号液氧罐含量低于 2.0，请安排补液氧"
- Given 科长把液氧阈值从 2.0 改为 2.5，When 下一班输入含量 2.3，Then 触发预警
- 边缘情况：字段未填 → 不触发预警，走必填校验

### 6.5 F5（P1）历史趋势与报表

**功能描述**：按日期/交班人筛选历史记录，查看任意一份完整交接单；提供水/电/气每日用量曲线、液氧消耗曲线、气瓶库存曲线；支持按月导出报表。

**验收标准**：

- Given 科长选择 8 月 1 日–8 月 24 日，When 查看用电量曲线，Then 曲线按日展示并可点击单点跳转当日交接单
- Given 导出 8 月报表，When 下载完成，Then Excel 包含每日用量明细与异常记录汇总（格式待确认）

### 6.6 F6（P1）科长管理后台

**功能描述**：科长专用 PC 后台，含记录管理（查看/导出/批注）、人员与排班管理（师傅账号开通停用、最小排班表"日期→人"——驱动接班人自动带出与漏交检测；登录提交人与当日排班不符时提示确认并留痕；不建换班审批流）、配置管理（预警阈值、气瓶基数、**电梯字典与运行时间表**、新风使用位置清单、锅炉清单）、审计日志（谁在何时改了什么配置）。

**验收标准**：

- Given 科长修改气瓶基数，When 保存，Then 审计日志记录修改人、时间、新旧值，且下一班填写即时生效
- Given 科长维护排班表，When 某日值班师傅提交交接单，Then 系统自动把次日值班师傅带为接班人
- Given 当日排班为张师傅，When 施师傅登录并提交交接单，Then 系统提示"今日排班为张师傅，您以施师傅身份提交，请确认实际当班"，确认后方可提交并留痕（audit_logs）
- Given 班次日期已过约定时点（默认次日 9:00，后台可调）仍无已提交记录，When 科长打开后台，Then 显示"应提交未提交"提醒（日期 + 排班人）

### 6.7 电梯时段核对设计（F1 表单 + F6 配置联动，v0.2 新增）

> 源自科长诉求："自定义多少电梯，可以设置他们是几点到几点的"。本方案在其基础上增加"预期状态自动核对"，把每班手写停运清单变成"配置一次、每天核对"。

**机制**：

1. **电梯字典（后台配置，可增删改）**：每台电梯一条记录——编号/名称（1 号、2 号、扶梯、5 号、8 号、人防电梯、发热门诊电梯……）+ 运行计划。
2. **运行计划三选一**：
   - 24 小时运行
   - 按时段运行：配置运行时段，例如扶梯 06:00–22:00 运行，其余时段停运
   - 长期停运：备注原因（如停用检修）
3. **交接表单自动核对**：系统按**核对时刻**（师傅在电梯板块实际核对的时间，系统自动记录）计算每台的"预期状态"。师傅看到的不是空白清单，而是逐台"预期：运行 / 预期：停运"，只需核对实际是否与预期一致——一致一键"确认"，不一致选择实际状态并填写说明。预期状态以核对时刻为准并锁定，**提交时不重算**，避免"核对时一致、提交时预期已翻转"的假预警。
4. **智能预警**：预期停运时段内电梯实际仍在运行（如扶梯深夜未停）→ 生成中等级预警；预期运行但停运且无说明 → 提示填写原因。分阶段说明：Phase 1 不一致项在交接单上标红并要求填写说明，预警中心与站内推送随 Phase 2 上线。
5. **核对时机**：建议电梯核对在**晚间巡检时完成**（可与 20:30 液氧读数顺路），使"停运时段内仍在运行"的违规在时段内可被发现；早晨提交前仅复核有无变化。系统只能发现"核对时刻"可见的状态，这是本方案的固有限制。

**价值**：填写量从"每班手写约 7 行清单"降为"逐台一键核对"；同时首次获得"电梯该停没停"的自动发现能力（限于核对时刻所处时段，见机制第 5 条）。

**验收标准**：

- Given 扶梯配置为 06:00–22:00 运行，When 师傅 23:00 打开电梯板块核对，Then 扶梯行显示"预期：停运"（预期状态按核对时刻计算）
- Given 师傅 21:00 核对扶梯为运行（与时点预期一致），When 23:00 提交交接单，Then 不因提交时刻已过 22:00 而生成"超出运行时段仍在运行"预警（预期状态锁定为核对时刻）
- Given 预期停运但师傅选择"实际仍在运行"，When 提交，Then 生成预警"扶梯超出运行时段仍在运行"
- Given 科室新增一台电梯，When 科长在后台添加并设置计划，Then 当日交接表单立即出现该电梯行
- 边缘情况：跨天时段（如 22:00–06:00 运行）→ 配置器支持跨零点表达

### 6.8 F7（P1）可选拍照留证（v0.2 新增）

**功能描述**：已确认保留。每个读数字段可选拍照（如水表、电表、液氧罐压力表），照片随交接单存档，详情页可查看。拍照不强制、不阻塞提交流程。

**验收标准**：

- Given 师傅在液氧罐压力字段点击拍照，When 拍摄完成，Then 照片缩略图挂在字段旁，随记录存档
- Given 师傅全程不拍照，When 提交，Then 流程正常不受影响
- 边缘情况：离线时拍照 → 照片随表单本地暂存，联网后一并同步

### 附录 A — 数据字典（十个板块逐字段，v0.2）

> 类型说明：数值=数字输入；状态=正常/异常选择；枚举=下拉；多选=勾选；文本=自由输入；自动=系统计算或带出

**基础信息**

| 字段 | 类型 | 填写方式 | 说明 |
| --- | --- | --- | --- |
| 日期 | 日期 | 自动 | **班次起始日**（24 小时班跨两个自然日，提交多发生在次日早晨；趋势、检索、报表一律以班次日期为准，不以提交日为准）；一天一条记录 |
| 交接时间 | 时间 | 自动 | 记录最终提交时刻（追溯用）；离线待同步的以同步成功时刻为准，异议退回后重新提交会更新此时间；电梯预期状态按核对时刻计算（见 6.7），不按此字段 |
| 交班人 | 文本 | 自动 | 当日值班师傅（登录账号） |
| 接班人 | 文本 | 自动+可改 | 按排班表带出次日值班师傅，特殊情况可改；**修改需填写原因并留痕**（责任界定关键字段） |

**一、水**

| 字段 | 类型 | 单位 | 填写方式 | 说明 |
| --- | --- | --- | --- | --- |
| 水表读数 | 数值 | 吨 | 手工 | 显示上一班值供比对；可选拍照 |
| 每日用水量 | 数值 | 吨 | 自动 | 本次−上一班读数 |

**二、电**

| 字段 | 类型 | 单位 | 填写方式 | 说明 |
| --- | --- | --- | --- | --- |
| 电表 1（如意线）读数 | 数值 | 度 | 手工 | 显示上一班值；可选拍照 |
| 电表 2（工贸线）读数 | 数值 | 度 | 手工 | 显示上一班值；可选拍照 |
| 每日用电量 | 数值 | 度 | 自动 | **两线差值之和（已确认）**，分线差值同屏展示 |
| 高配房是否正常 | 状态 | — | 选择 | 异常时必填备注，触发预警 |

**三、天然气**

| 字段 | 类型 | 单位 | 填写方式 | 说明 |
| --- | --- | --- | --- | --- |
| 表 1（主卡）剩余量 | 数值 | 立方米 | 手工 | 剩余量递减 |
| 表 2（副卡）剩余量 | 数值 | 立方米 | 手工 | 剩余量递减 |
| 每日天然气用量 | 数值 | 立方米 | 自动 | 剩余量减少值合计 |

**四、医用气体（最重板块）**

> 注：液氧"含量/用量"的计量单位待与现场仪表核实——原案例记录为 L，与医院液氧储罐量级明显不符（疑似 m³、吨或液位计米数）。下表暂沿用原记录，单位确认后全局替换（含阈值 2.0 与时段降幅 0.8）。

| 字段 | 类型 | 单位 | 填写方式 | 说明 |
| --- | --- | --- | --- | --- |
| 1 号液氧罐 8:30 含量 / 压力 | 数值 | L / MPa | 手工 | 两个时点读数，当班师傅一人测两次；8:30 填写时带出昨日 20:30 值供比对 |
| 1 号液氧罐 20:30 含量 / 压力 | 数值 | L / MPa | 手工 | 同上 |
| 2 号液氧罐 8:30 含量 / 压力 | 数值 | L / MPa | 手工 | 同上 |
| 2 号液氧罐 20:30 含量 / 压力 | 数值 | L / MPa | 手工 | 同上 |
| 液氧站高压氧舱回路出口压力 | 数值 | MPa | 手工 | |
| 液氧日间用量 | 数值 | L | 自动+可改 | 在用罐 8:30–20:30 含量差（沿用纸质口径），可修正 |
| 液氧夜间用量 | 数值 | L | 自动 | 昨日 20:30 → 今日 8:30 跨记录差值（建议新增，待确认）；与日间用量相加为全天用量 |
| 液氧站出口压力 | 数值 | MPa | 手工 | |
| 使用罐号 | 枚举 | — | 选择 | 1 号 / 2 号 |
| 40L 氧气满瓶 | 数值 | 瓶 | 手工 | 基数 40（后台配置） |
| 10L 氧气满瓶 | 数值 | 瓶 | 手工 | 基数 20 |
| 6L 氧气满瓶 | 数值 | 瓶 | 手工 | 基数 20 |
| 二氧化碳满瓶 | 数值 | 瓶 | 手工 | 基数 20 |
| 肺功能气体满瓶 | 数值 | 瓶 | 手工 | 基数 1 |
| 瓶氧汇流排进口压力 | 数值 | MPa | 手工 | |
| 二氧化碳（手术室）出口压力 | 数值 | MPa | 手工 | |
| 负压、空气是否正常 | 状态 | — | 选择 | 异常触发预警 |

**五、供暖/冷系统**

| 字段 | 类型 | 单位 | 填写方式 | 说明 |
| --- | --- | --- | --- | --- |
| 锅炉房是否正常 | 状态 | — | 选择 | |
| 锅炉是否运行 | 枚举 | — | 选择 | 运行/停机 |
| 使用锅炉号 | 枚举 | — | 选择 | 停机时置灰不填 |
| 出水温度 | 数值 | °C | 手工 | 停机时置灰 |
| 回水温度 | 数值 | °C | 手工 | 停机时置灰 |
| 制冷机房是否正常 | 状态 | — | 选择 | |
| 制冷是否运行 | 枚举 | — | 选择 | 是/否 |

**六、生活水泵（热/冷）**

| 字段 | 类型 | 单位 | 填写方式 | 说明 |
| --- | --- | --- | --- | --- |
| 1 号楼热水：设置温度 / 出口水温 | 数值 | °C | 手工 | 偏离设定 ±3° 提示 |
| 3 号楼热水：设置温度 / 出口水温 | 数值 | °C | 手工 | 同上 |
| 1 号楼水泵：出口水压 | 数值 | MPa | 手工 | |
| 1 号楼水泵：水位 / 高度 | 状态+数值 | m | 选择+手工 | |
| 3 号楼水泵：出口水压 / 水位 / 高度 | 同上 | — | 同上 | |

**七、新风/空调系统**

| 字段 | 类型 | 填写方式 | 说明 |
| --- | --- | --- | --- |
| 性能情况 | 状态 | 选择 | 正常/异常 |
| 使用位置 | 多选 | 勾选 | 候选清单后台配置（如 1 号楼 5–8 层、急诊区域、3C、4C） |

**八、节能减排**

| 字段 | 类型 | 填写方式 | 说明 |
| --- | --- | --- | --- |
| 节能减排事项 | 文本 | 手工 | 无则可留空 |

**九、电梯（字典化 + 时段核对，v0.2 重设计）**

| 字段 | 类型 | 填写方式 | 说明 |
| --- | --- | --- | --- |
| 电梯字典 | 配置 | 后台维护 | 编号/名称 + 运行计划（24 小时 / 按时段运行 / 长期停运），详见 6.7 |
| 每台电梯实际状态 | 核对 | 选择 | 系统按核对时刻给出"预期状态"（见 6.7），师傅核对实际是否一致；不一致需说明 |

**十、其它**

| 字段 | 类型 | 填写方式 | 说明 |
| --- | --- | --- | --- |
| 未完成/需强调的交接事项 | 文本 | 手工 | 有内容时接班人必须逐条确认 |

---

## 7. 约束与风险

**技术约束**：移动端优先（H5，兼容主流手机浏览器）；管理后台为 PC Web；**必须支持离线填写与自动同步**（地下表房弱信号为常态场景；"提交成功"以数据上传至服务器为准，见 6.1）；数据长期留存（建议至少 3 年）。

**账号与对接**：账号密码登录、实名账号；暂不对接企业微信/钉钉/短信网关，消息提醒走站内渠道，架构上预留第三方对接接口（已确认）。

**部署约束**：建议医院内网私有化部署（见附录 B），离线同步方案需兼容内网环境。

**设计约束（不可妥协）**：

- 单次交接 10 分钟内完成——填写效率高于一切功能花哨度
- 适配中年师傅：大字号、大按钮、数值字段自动弹数字键盘、单位全程显式标注
- 预警必须"可解释"：每条预警说明命中了哪条规则、阈值是多少

**数据与安全**：数据涉及医院后勤保障运行（含医用氧气等生命支持资源），按医院等级保护要求管理；配置修改与数据覆盖全部留痕；照片存储占用与留存策略需规划（见剩余待办）。

**关键风险表**：

| 风险 | 可能性 | 影响 | 缓解措施 |
| --- | --- | --- | --- |
| 师傅嫌手机填写比手写慢，弃用系统 | 中 | 高 | MVP 首周跟班试用；自动回填/置灰不变项；电梯一键核对减负 |
| 地下室信号弱导致数据丢失 | 中 | 高 | 离线填写 + 本地草稿双保险，同步状态可见 |
| 阈值设置不当导致预警疲劳 | 中 | 中 | 阈值后台可配 + 每月复盘预警准确率（反指标约束） |
| 双轨期数据口径不一致 | 中 | 低 | 双轨并行两周，逐日比对纸电数据后再单轨 |
| 交接责任争议 | 低 | 中 | 逐项确认 + 电子签名 + 异议流程全程留痕 |
| 师傅间共用账号，电子签名失去实名基础，责任界定失效 | 中 | 高 | 账号实名一人一号；签名时显示当班人姓名并二次确认；审计日志记录登录设备；培训明确"谁值班谁登录" |
| 仅站内提醒，师傅/科长不登录则预警与待办不可见 | 中 | 中 | 双轨期由科内微信群人工转发每日预警摘要（过渡措施）；站内未读角标 + 每日固定时点提醒汇总；上线稳定后视情况重估第三方推送 |

---

## 8. 发布计划

**推荐顺序**：先发"替代纸张"的最小闭环，再上智能能力。

**Phase 1（MVP）**：F1 表单填写（含离线）+ F3 用量自动计算 + F2 交接确认（含排班带出接班人）+ 电梯字典核对 + F6 精简版（人员、排班、记录管理、**电梯字典与运行时间表维护**）。电梯不一致项 Phase 1 在交接单上标红并要求填写说明，预警中心与站内推送随 Phase 2 上线；Phase 1 的标红项（状态异常、电梯不一致等）同样纳入接班人逐条"已知晓"确认，与 Phase 2 预警确认操作保持一致。价值闭环：纸彻底停用，记录电子化。

**Phase 2**：F4 阈值预警 + F5 历史趋势报表 + F7 拍照留证 + F6 完整配置中心。价值闭环：数据开始产生决策价值。

**上线考虑**：

- 上线前与科长共同确认全部阈值、气瓶基数、电梯字典与时间表、新风口位清单
- 双轨并行两周（纸电同时填，逐日比对），验证一致后转单轨
- 全员培训：一次 30 分钟现场演示 + 一张手机操作卡片；明确"提交后看到'已同步'方可下班"（离线数据仅存本机）
- 上线后头两周监测：单次填写耗时、草稿放弃率、离线同步成功率、预警准确率

---

## 附录 B — 部署方案对比

| 方案 | 说明 | 优点 | 缺点 |
| --- | --- | --- | --- |
| **A 医院内网私有化部署（推荐）** | 系统部署在院内服务器，手机经院内 WiFi 访问 | 数据不出院，安全性最高；一次性投入无持续费用；师傅全程在院内，内网足够 | 院外访问需 VPN；需信息科配合准备服务器 |
| B 公有云部署 | 部署在阿里云/腾讯云 | 无需自备服务器 | 院区运行数据出院需过安全审批；持续付费 |
| C 混合/脱敏上云 | 云端部署+加密脱敏 | 折中 | 架构复杂，对本规模系统不划算 |

建议选 A。若未来医院统一移动入口（企业微信/钉钉），可挂为内部 H5 应用，接口已预留。

---

## 剩余待办（第 1–4 项为 v0.2.1 新增，其中 1、4 建议优先闭环）

1. **液氧计量单位核实（新增，优先）**：现场确认液氧罐含量仪表的单位与量程——原案例记录为 L，与医院储罐量级明显不符（疑似 m³、吨或液位米）；确认后同步修正阈值 2.0、时段降幅 0.8 及全部用量字段的单位与数值
2. **液氧夜间用量口径确认（新增）**：是否新增"昨日 20:30 → 今日 8:30"夜间差值计算，补齐全天用量与夜间防泄漏监测（见 6.3、6.4）
3. **轮值师傅人数与名单（新增）**：影响账号开通、培训安排与排班模块设计
4. **部署方案确认（新增，优先）**：建议方案 A（内网私有化）；需信息科确认服务器资源与院内 WiFi 覆盖（含地下表房等弱信号点位）——手机仅能经院内网络访问系统，为 Phase 1 前置条件
5. **电梯时间表初始配置**：请提供每台电梯的编号与运行时段（例：扶梯 06:00–22:00 运行），我们录入系统
6. 压力类预警初始区间定稿（液氧罐压力、汇流排、各出口压力），其余阈值已确认或后台可调
7. 照片留存时长与查看权限（建议：随记录保存 1 年，仅科长可调阅原图）
8. 数据留存年限确认（建议 ≥ 3 年）

---

## 薄弱章节提示

- **交互设计稿未生成**：移动端表单、电梯核对、确认流程的页面设计待产出
- **埋点清单未定义**：成功度量的数据采集方案待细化
- **配置初始值**：电梯时间表与压力区间以总务科提供为准
- **待核实项**：液氧计量单位与夜间用量口径（剩余待办 1、2）影响预警与报表的全部数值，需优先闭环
