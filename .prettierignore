node_modules/
dist/
.vite/
coverage/
playwright-report/
test-results/
*.local
.env
.env.*
pnpm-lock.yaml

# docs/ 与 demo/ 是总务科拍板的定稿文档，不参与机器格式化，避免无意义重排行
demo/
docs/
scripts/

# IDE/工具产物（已入 .gitignore），不参与格式化
.qoder/

# drizzle-kit 生成的迁移快照与日志（机器产物，每次 db:generate 重写，格式化无意义且会反复冲突）
apps/api/drizzle/meta/
